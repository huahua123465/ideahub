# 本地验收清单

把下面这段整个复制，粘贴给能在这台机器上执行命令的会话（Claude Code / 终端都行）。
它会跑完全部验证并把结果贴出来。

---

## 复制这段

```
在 D:\work\共享知识库 里跑本地验收，按顺序执行并把每一步的输出贴出来：

1. node -v                                          （需要 20 以上）
2. docker compose -f docker-compose.dev.yml up -d    （起数据库；没有 Docker 就跳到第 6 步）
3. npm install                                       （只装一个包 pg）
4. npm run db:init
5. npm run db:seed
6. npm start                                         （另开一个终端，保持运行）
7. npm run test:api                                  （期望：通过 58   失败 0）
8. 浏览器打开 http://localhost:3000 点一遍：
   提交灵感 → 投票 → 打开详情发条评论 → 否决（先不填理由看是否被拦）→ 采纳并选负责人
   → 切正式库看新编号 → 切统计看板

跑完告诉我：第 7 步的通过/失败数字，以及第 8 步里哪一步和预期不一样。
如果任何一步报错，把完整报错贴出来，不要自己改代码。
```

---

## 每一步预期看到什么

| 步骤 | 预期 |
|---|---|
| `npm install` | 只装 `pg` 一个包，没有其他依赖 |
| `npm run db:init` | `完成，现有表： idea_activities, idea_comments, idea_votes, ideas, users` |
| `npm run db:seed` | 用户 96 人 / 灵感池 9 条 / 已否决归档 11 条 / 正式库 6 条 / 讨论 93 条 |
| `npm start` | 打印「定时任务   热度每 15 分钟重算，超期归档每天一次」 |
| `npm run test:api` | 最后一行 `通过 58   失败 0` |

## 最需要留意的一件事

云端环境的 npm 被安全策略挡住了，装不了包，所以**生产用的 `pg` 驱动是唯一没能实测的部分**
（那边全程用的是免依赖的 psql 驱动，SQL 逻辑一致）。

`npm start` 之后如果看到这类报错：

```
bind message supplies N parameters, but prepared statement requires M
```

说明还有某处 SQL 的占位符和传参个数对不上。把完整报错和触发它的接口路径发我，我来修。
（这类问题已经在 psql 驱动里加了同样严格的校验，58 项自测全过，理论上不该再出现，但值得盯一眼。）

## 想更省事

直接双击 `启动.bat`，它会把第 2–6 步自动做完并打开浏览器。
只想看界面不碰数据库，双击 `启动-仅看界面.bat`。
