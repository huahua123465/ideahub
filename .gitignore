# ============================================================
# 仓库保存代码和可公开的配置模板；真实运行配置与密钥只保留在部署环境。
# ============================================================

# ---- 本地/生产环境配置与密钥：只提交脱敏模板 ----
.env
.env.*
!.env.example

# ---- 装出来 / 生成出来的，恢复时重新生成即可 ----
node_modules/
web/dist/

# ---- Postgres 的数据目录：不进 git，用 backup/ideahub-db.sql 代替 ----
#
# 不是"嫌它大"，是**照搬它恢复不出来**：
#   1. 数据库在跑的时候复制这个目录，拿到的是撕裂的快照
#      （页面写到一半 + WAL 对不上），还原时很可能起不来，
#      而你只会在真的需要它的那天才发现。
#   2. pg_wal 里两个 16MB 的文件每天都在变，每次备份都要重新塞 32MB 进仓库，
#      一个月就把仓库撑到 1GB 以上。
#   3. 它绑死 PostgreSQL 的大版本和 CPU 架构，换台机器不一定认。
#
# backup/ideahub-db.sql 是 pg_dump 出来的**逻辑备份**：0.29MB，
# 一条命令就能还原到任何一台 PostgreSQL 16 上，实测逐表行数一致。
# 数据一条不少，而且真的能恢复。
data/pg/

# ---- Caddy 的访问日志：恢复时用不上，而且每天都在滚动 ----
data/caddy/

# ---- 跑出来的临时文件 ----
*.log
scripts/.uidiff/
scripts/推送失败待补.jsonl

# 长任务构建契约与评审运行记录（只用于本地协作，不进入产品仓库）
.agents/runtime/harness/
