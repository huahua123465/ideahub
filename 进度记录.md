# 进度记录

> 每完成一个任务往下追加一段。**只追加，不覆盖。**
> 会话中断、额度用尽、误关窗口后，从最后一段的「下一步」接着做即可。

格式：任务号 / 时间 / 做了什么 / 动了哪些文件 / 怎么验证 / 下一步

---

## T0 项目框架 ✅

**时间**：2026-08-20

**做了什么**

搭好整个项目的目录骨架，确定技术栈并写清楚理由。

技术栈做了一次关键调整：**放弃 React + Vite，改用原生 ES Module + 零构建**。
原因是 UI 必须和客户已确认的演示原型一模一样 —— 直接沿用原型的 CSS 与 DOM 结构，
比重写成 React 组件再回头对齐要可靠得多，也省掉整条构建链。
最终全项目只有一个 npm 依赖：`pg`。

**动了哪些文件**

```
README.md              项目说明、三种启动方式、目录结构、协作约定
进度记录.md            本文件
docs/任务清单.md       T0–T17 共 18 个任务的拆解
docs/架构设计.md       数据模型、状态机、API 清单、部署方案（选型阶段已写好）
docs/UI原型-基准.html  客户确认过的演示原型，作为 UI 比对的基准
```

建好的空目录：`server/src/{db,routes,lib}`、`web/src/views`、`scripts`

**怎么验证**

目录结构齐全，`docs/UI原型-基准.html` 用浏览器打开可以正常点击。

**下一步**

T1 数据库层：写 `schema.sql` 和 `db/` 驱动层。

---

## T1 数据库层 ✅

**时间**：2026-08-20

**做了什么**

写完建表 SQL 和数据库驱动层，并在真实 PostgreSQL 16 上跑通。

几个设计点：

- **投票用联合主键防重复**（`PRIMARY KEY (idea_id, user_id)`），不在应用层做「先查后写」——
  并发双击时应用层判重必漏，数据库约束是唯一靠得住的闸门。
- **票数和评论数用触发器同步**到 `ideas` 表的物化列，列表查询就不用每次 COUNT 子表。
- **`idea_activities` 审计表**记录每一次状态变更和理由，让「这条灵感当初为什么被否决」半年后还查得到。
- **热度函数带时间衰减**（`(票数 + 评论数×2) / (小时数+2)^1.5`），避免最早提的永远霸榜。
- 中文查重用 `pg_trgm` 而不是 `zhparser`，省掉一个需要编译安装的扩展。

驱动层做了两个实现，接口完全一致：

- `db/pg.mjs` —— 生产用，node-postgres 连接池。顺手把 BIGINT 从字符串转回数字。
- `db/psql.mjs` —— 备用，常驻一个 psql 交互进程，零 npm 依赖。本项目在无网环境下的测试就靠它。

**动了哪些文件**

```
server/src/schema.sql        建表、索引、触发器、热度函数
server/src/db/index.mjs      驱动选择 + 统一出口（query / tx / close）
server/src/db/pg.mjs         生产驱动
server/src/db/psql.mjs       免依赖备用驱动
server/src/db/init.mjs       建表脚本，支持 --drop 重建
server/src/lib/env.mjs       极简 .env 读取，不依赖 dotenv
package.json                 加了 db:init / db:seed / db:reset 脚本
```

**怎么验证**

```bash
npm run db:init --drop
```
输出 5 张表：`idea_activities, idea_comments, idea_votes, ideas, users`。已在 PostgreSQL 16.13 实测通过。

**下一步**

T2 种子数据。

---

## T2 种子数据 ✅

**时间**：2026-08-20

**做了什么**

灌入演示视频里那批内容：9 条灵感池、6 条正式库、6 条讨论、完整的流转记录。

一个刻意的选择：**票数不是硬写进 `vote_count` 的**，而是造了 90 个虚拟同事，
真实地往 `idea_votes` 插行，让触发器把计数加上去。这样种子数据本身就顺带验证了触发器，
而且 `idea_votes` 和 `vote_count` 不会对不上。打乱用户顺序用的是按 idea_id 做种子的伪随机，
所以每次 seed 结果完全一致。

**动了哪些文件**

```
server/src/seed.mjs
```

**怎么验证**

```bash
npm run db:seed
```
预期输出：用户 96 人 / 灵感池 9 条 / 正式库 6 条 / 讨论 6 条。
票数应与演示视频一致：沙箱环境 52、周会 41、工单打标 34、构建流水线 31。已实测通过。

**下一步**

T3 后端 HTTP 骨架。

---

## T3–T7 后端全部接口 ✅

**时间**：2026-08-20

**做了什么**

后端写完了，13 个接口，56 项自测全绿。没有用 Web 框架 —— 路由就十来条，
引一个框架换来的是一条依赖和一份长期升级负担，自己写了 40 行的极简路由。

**踩到并解决的一个真坑（重要）**

`pg_trgm` 在 C locale 下**对中文一个 trigram 都切不出来**，`similarity()` 恒等于 0，
提交查重会永远返回空 —— 而且不报错，是静默失效。这种问题留到线上会非常难查。

解决办法：不依赖数据库 locale，自己实现 `cn_similarity()` —— 按字切二元组算 Jaccard 相似度。
英文仍走 `pg_trgm`，`title_similarity()` 取两者的大值。实测：

| 对比 | 相似度 |
|---|---|
| 「用大模型给工单打标签」vs「用大模型自动给客服工单打标签」 | 0.467 |
| `single sign on` vs `unified single sign-on` | 0.619 |
| 「把周会改成异步文档」vs「官网加沙箱环境」 | 0.000 |

区分度足够，阈值定在 0.2。

**几个实现上的取舍**

- **投票用 `ON CONFLICT DO NOTHING ... RETURNING`**：有返回行说明是新票，没有说明已投过 → 撤票。
  全程靠数据库的唯一约束判重。自测里同一用户并发发 8 次投票，票数始终是 0 或 1。
- **采纳用 `SELECT ... FOR UPDATE` 行级锁**：自测里 5 个评审并发点采纳，只有 1 个成功，
  其余拿到锁后看到状态已是 adopted，被状态机挡掉，编号也只生成一个，没有撞号。
- **否决必须填理由**，接口层强制。石沉大海比被否决更伤人，这条规则的全部意义就在这里。
- **通知放在事务外**：群机器人挂了不该让状态流转回滚。
- **登录留了替换点**：现在从 `X-User-Id` 头取人，接企业微信/飞书时只改 `lib/auth.mjs` 一个文件，
  业务代码一行不用动。

**动了哪些文件**

```
server/src/index.mjs             入口：http 服务 + 静态托管 + CORS + 优雅退出
server/src/router.mjs            极简路由（:param 支持、405 区分）
server/src/lib/http.mjs          请求体读取、JSON 响应、错误类型、字段校验
server/src/lib/dto.mjs           数据库行 → 前端对象，集中一处
server/src/lib/auth.mjs          当前登录人 + 评审权限断言
server/src/routes/ideas.mjs      列表 / 详情 / 提交 / 编辑 / 查重
server/src/routes/votes.mjs      投票（点一次投，再点一次撤）
server/src/routes/comments.mjs   讨论
server/src/routes/status.mjs     状态流转（行级锁 + 状态机 + 编号 + 审计）
server/src/routes/stats.mjs      统计看板 + 热度重算
server/src/schema.sql            追加了中文相似度函数
scripts/test-api.mjs             56 项接口自测
```

**怎么验证**

```bash
npm start            # 另开一个终端
npm run test:api
```
预期最后一行：`通过 56   失败 0`。已实测通过。

**下一步**

T8 前端骨架 —— 把演示原型的 CSS 原样搬过来，接上真实接口。

---

## T8–T14 前端全部界面 ✅

**时间**：2026-08-20

**做了什么**

前端写完了。**没有用 React，也没有构建步骤** —— 这是为了 UI 保真度做的取舍：
`web/styles.css` 是从客户确认过的演示原型里**逐字抄出来的 267 行**，DOM 结构也一一对应。
重写成 React 组件再回头对齐，只会引入一堆本来不存在的偏差。

模块划分：

```
web/index.html          结构与原型一一对应
web/styles.css          原型 CSS 逐字搬运（UI 唯一真源）
web/src/main.js         入口，把各视图接起来
web/src/api.js          接口层，后端不可用时自动降级到 mock
web/src/mock.js         内置演示数据，等价于一个假后端
web/src/util.js         头像色、相对时间、HTML 转义
web/src/toast.js        提示条
web/src/apilog.js       左下角的 API/SQL 实时调用浮层
web/src/views/pool.js   灵感池
web/src/views/formal.js 正式库
web/src/views/stats.js  统计看板
web/src/views/drawer.js 详情抽屉
web/src/views/modal.js  提交弹窗
```

**几个值得记下来的点**

- **后端没起也能看界面**。`api.js` 启动时探一次 `/api/health`，探不到就整场走 `mock.js`。
  所以「给人看效果」和「真开发」用的是同一份前端代码，不用维护两套。
- **投票不整页重渲染**，只改那一张卡片的数字。否则用户点个赞，正在看的列表会因为热度重排而跳走。
- **HTML 一律走 `esc()` 转义**。标题是用户输入的，里面可能有 `<`、`&`。
- **漏斗的配色是有讲究的**：四个阶段用同一个蓝色 ramp 由浅到深，不混色相。
  混色相会让人误以为不同颜色代表不同类别，而它们其实是同一件事的四个阶段。
  窄条的标签放到条外，否则会被裁掉。这两条都写进了 `stats.js` 的注释，别随手改回去。

**动了哪些文件**

上面列出的 13 个前端文件，加上 `scripts/serve-web.mjs`（纯前端静态服务器）和
`scripts/dev.mjs`（一条命令同时起前后端）。

**怎么验证**

```bash
node scripts/serve-web.mjs     # 零依赖，只看界面
npm run dev                    # 前后端一起起
```

**下一步**

T15 UI 一致性验收 —— 得拿数据说话，不能只靠肉眼。

---

## T15 UI 一致性验收 ✅

**时间**：2026-08-20

**做了什么**

写了 `scripts/ui-check.mjs`：把真实应用逐屏截图，和 `docs/UI原型-基准.html` **逐像素**比对，
输出差异比例和一张把差异标红的图。

**最终结果（阈值 1.5%，五屏全过）**

| 界面 | 像素差异 |
|---|---|
| 灵感池 | 0.030% |
| 正式库 | 0.001% |
| 统计看板 | 0.001% |
| 提交弹窗 | 0.017% |
| 详情抽屉 | 0.043% |

**过程中发现并修掉的真问题**

比对不是一次就绿的，第一轮差异高达 6.6%。逐个查下来，其中有几个是真 bug：

1. **分类为 0 时整行从图上消失**。`GROUP BY category` 只会返回有数据的分类，
   「其他」一条灵感都没有，那一行就没了。改成先 UNION 出固定的五个分类再 LEFT JOIN ——
   「没人往这个分类提灵感」本身是信息，不该从图上消失。
2. **采纳率显示 100%**。种子数据里一条被否决的灵感都没有，`6/(6+0)` 就是 100% —— 一眼假。
   补了 8 条被否决 + 3 条已归档的灵感，每条都带真实的否决理由，采纳率变成 43%。
   顺带一提，「提了没被采纳」才是灵感库的多数情况，这也是采纳率这个指标有意义的前提。
3. **详情页最有信息量的讨论被挤到后面**。补充的短评时间戳随机落在 18–78 小时内，
   会盖过手写的那三条。改成一律落在 16 小时内，保证有内容的那几条排在最前面。

剩下的差异属于**数据而非样式**，比对前统一归一化掉了，并在脚本里写清楚了原因：
卡片排序（真实热度 vs 静态稿的手写顺序）、统计数字、「我投过票」这类用户状态、
以及基准原型自身的一处自相矛盾（卡片写着 12 条讨论，抽屉里只画了 3 条）。

**动了哪些文件**

```
scripts/ui-check.mjs        比对脚本
scripts/record-demo.mjs     录制完整流程演示视频（给非技术同事看用）
server/src/routes/stats.mjs 分类为 0 也要出现
server/src/seed.mjs         补否决/归档数据、评论数对齐演示、评论时序修正
web/src/util.js             相对时间两周内一律说「天」
web/src/views/stats.js      副标题改成真实数字
scripts/test-api.mjs        评论数断言跟着更新
```

**怎么验证**

```bash
npm run test:ui
```
预期：`最大差异 0.043%   不通过 0 屏`。截图和差异图在 `scripts/.uidiff/`。

**下一步**

T16 端到端联调 + T17 部署配置。

---

## T16–T17 端到端联调与部署配置 ✅

**时间**：2026-08-20

**做了什么**

对着真实 PostgreSQL 16 跑完整流程，并录了一段 58 秒的视频作为证据：
提交（含实时查重）→ 投票 → 打开详情 → 发表讨论 → 采纳立项 → 进正式库 → 统计看板。
全程无报错，左下角浮层里能看到真实的接口路径和状态码（`POST /api/ideas/3/comments 201`、
`PATCH /api/ideas/3/status 200`）。

接口自测同步涨到 **58 项，全绿**。

部署配置齐了：

```
Dockerfile              node:22-alpine，只装一个 pg 依赖，带 healthcheck
docker-compose.yml      生产：db + api + caddy，内存限额合计 ≈ 900M
docker-compose.dev.yml  开发：只起一个 postgres，应用跑宿主机
Caddyfile               自动 HTTPS，访问日志滚动
scripts/backup.sh       每日备份，产物小于 1KB 直接判失败并删掉
scripts/cron.mjs        热度重算 + 超期归档
.dockerignore
```

备份脚本里那个「小于 1KB 就报错」的判断值得说一句：**一个静默失败的空备份比没有备份更危险**，
因为它会让人以为自己有备份。

**怎么验证**

```bash
npm start && npm run test:api     # 58 项全过
npm run test:ui                   # 5 屏全过
docker compose config             # 部署配置语法检查
```

**下一步**

代码已全部就位。你那边需要做的：
1. 起数据库：`docker compose -f docker-compose.dev.yml up -d`
2. `npm install`（只装一个 pg）→ `npm run db:init` → `npm run db:seed` → `npm run dev`
3. 确认无误后再谈上服务器的事

后续可做（都还没做）：企业微信/飞书扫码登录、灵感编辑的富文本、
附件上传、正式库的进度更新界面、移动端适配。

---

## 补丁：修掉一个只会在生产驱动上炸的 bug ⚠️ 重要

**时间**：2026-08-20

**问题**

灵感池列表接口把分页参数一起传给了 count 查询：

```js
// 错误写法
const { rows } = await query(`${IDEA_SELECT} ${clause} LIMIT ${arg(pageSize)} OFFSET ${arg(off)}`, args);
const { rows: cnt } = await query(`SELECT count(*) FROM ideas i ${clause}`, args);  // ← args 多了两个
```

`psql` 驱动是字符串替换，多传的参数根本用不上，**静默通过**。
但生产用的 `pg` 驱动走的是扩展查询协议，**参数个数必须严格一致**，会直接报：

```
bind message supplies 5 parameters, but prepared statement requires 3
```

也就是说：本地开发全绿，一换到服务器上，灵感池首页直接 500。这类问题最难查，
因为「我本地明明是好的」。

**修法**

1. WHERE 的参数和「当前用户 / 分页」参数分开装，count 查询只拿它真正用到的那几个。
2. `IDEA_SELECT` 改成 `ideaSelect(userParam)` 函数 —— 动态拼 WHERE 时用户 id 的位置不固定，不能写死 $1。
3. **在 psql 驱动里加上和 pg 一样严格的参数校验**：占位符个数与传参个数不一致就直接抛错。
   这样以后同类问题在开发阶段就会炸，而不是留到线上。

第 3 条是这次真正的收获 —— 光修掉这一处不够，得让开发环境和生产环境**错得一样早**。

**验证**

打开严格校验后重跑全部 58 项接口自测，仍然全绿，说明代码里没有别处存在同类问题。
UI 比对也复跑了一遍，5 屏全过，最大差异 0.043%。

**动了哪些文件**

```
server/src/lib/dto.mjs        IDEA_SELECT → ideaSelect(userParam)
server/src/routes/ideas.mjs   列表接口参数分离
server/src/db/psql.mjs        加严格参数校验
```

---

## T18 评审弹窗 + 一键启动 ✅

**时间**：2026-08-20

**做了什么**

**1. 干掉了原生 `prompt()`。** 采纳和否决现在各有一个正式弹窗：

- **采纳并立项**：可以指定负责人（默认挂在提交人名下 —— 提出想法的人通常最清楚怎么做）
  和初始进度，确认后生成正式编号进正式库。
- **否决**：理由必填，前后端双重校验。文案直接写了「石沉大海比被否决更伤人」，
  这是产品意图，不是装饰。

两个弹窗完全复用原型的 `.modal` 样式，所以 UI 比对仍然是 5 屏全过。

**2. 分类筛选现在可以取消。** 筛选条里分类没有「全部」这一项，
之前用户一旦点进「技术」就出不来了。改成再点一次选中的分类即取消。排序则始终保持一个选中。

**3. 一键启动。** `scripts/setup.mjs` 六步走完：检查 Node → 准备 .env → 装依赖 →
连数据库（连不上自动尝试 Docker） → 建表灌数据（只在库是空的时候） → 起服务开浏览器。
每一步失败都给出下一步怎么办，而不是甩堆栈。Windows 配了两个 bat 双击即用。

实测过三条路径：库是空的（建表 + 灌数据）、库已有数据（跳过）、依赖装不上（自动退到 psql 驱动）。

**4. 给 `db:seed` 加了防呆。** 这个脚本会 TRUNCATE 整个库 ——
在生产上手滑跑一次，所有真实灵感就没了。现在会先报清楚要清空多少条，
且 `NODE_ENV=production` 下必须显式加 `--force` 才放行。

**动了哪些文件**

```
web/index.html            两个评审弹窗
web/src/views/review.js   新增：采纳/否决弹窗逻辑
web/src/views/drawer.js   改为抛事件，不再自己弹 prompt
web/src/main.js           接线 + 分类筛选可取消
web/src/api.js            补 PATCH /api/ideas/:id
web/src/mock.js           mock 同步支持 PATCH
scripts/setup.mjs         新增：一键启动
scripts/record-demo.mjs   演示流程扩到覆盖全部功能
server/src/seed.mjs       生产防呆
package.json              npm test / npm run setup / npm run demo
启动.bat / 启动-仅看界面.bat
README.md                 补一键启动说明
```

**怎么验证**

```bash
npm test        # 重灌种子 → 58 项接口自测 → 5 屏 UI 比对
```

实测：接口 `通过 58   失败 0`；UI `最大差异 0.043%   不通过 0 屏`。

**留给你决定的事**（见回复里的清单）

---

## 自查：把「说过的」和「做到的」逐条对了一遍 ⚠️

**时间**：2026-08-20

起因是被问「上面写的功能全都实现了吗」。没凭印象回答，写脚本把文档承诺、聊天里的说法
和实际代码逐条比对了一遍。**发现 7 处对不上，其中 2 个是真 bug。**

### 真 bug（已修）

**1. 新提交的灵感会沉到列表最底下 —— 提了就看不见。**

`hot_score` 公式是 `(票数 + 评论数×2) / (小时数+2)^1.5`，新灵感票数和评论数都是 0，
分子恒为 0，热度就是 0，在「按热度」下必然排最后一名。实测确认：新提交的排第 10（最后）。
更糟的是提交后那个「新卡片高亮闪一下」的动画，闪的是一张用户根本看不到的卡片。

修法是给分子加一个基础分 `+1`（Hacker News 靠「作者自动投自己一票」拿到同样的东西）。
新灵感开局 1/2^1.5 ≈ 0.35，稳稳排第一，再随时间衰减。另外加了触发器在插入和计数变化时
立刻算一次，不用等定时任务。实测：新提交的现在排第 1。

**2. 定时任务在默认部署下根本不会跑。**

`/api/maintenance/recalc-hot` 和 `/api/maintenance/archive-stale` 两个接口是有的，
但 `docker-compose.yml` 里没有任何东西去调它们 —— 我却在文档和对话里说「热度会衰减」
「90 天自动归档」，等于承诺了一件不会发生的事。

修法是把调度器直接放进后端进程（`ENABLE_SCHEDULER=false` 可关）。这样 `docker compose up`
之后它就真的在跑，不依赖任何人记得去配 crontab。

### 说法不准确（已改）

**3. 「否决理由会通知到提交人」是假的。** 系统里根本没有针对个人的通知，
`notify()` 只是发到群机器人。弹窗上的这句话是在产品界面里许一个做不到的承诺，
比代码 bug 更糟。文案改成「记进流转记录并播报到群」。

**4. 自动归档是静默的，什么通知都没发。** 悄悄把人的灵感收走，比不收更伤人。补上了群播报。

**5. 架构文档和实现对不上三处**：写了 `search_vector TSVECTOR` 字段（实际没建，
查重走的是自研的 `cn_similarity()`）、写了 Redis 和对象存储（实际都没用）。
文档已按实现改写，并把中文搜索那个坑单独写了一节。

### 功能缺口（已补一个）

**6. 正式库的行点不开详情。** 灵感被采纳后，它的讨论和流转记录就再也翻不出来了。
已补上行点击（点「查看 ↗」仍然是去开文档）。

### 仍然没做（留给你定，见回复清单）

**7. 已否决 / 已归档的 11 条灵感在界面上完全看不到。** 后端支持 `?status=rejected`，
但筛选条里没有对应的按钮。加按钮会改动客户确认过的筛选行布局，所以没擅自加。

另外：评论回复（后端支持 `parentId`，前端没入口）、草稿状态、正式库进度更新入口、
附件上传、企业微信登录，都还是空的。

**验证**

```
npm test
  接口自测   通过 58   失败 0
  UI 比对    最大差异 0.043%   不通过 0 屏
```

---

## T21 本机开发环境搭建 ✅

**时间**：2026-08-20

**做了什么**

在开发机上把环境从零装好并跑起来了。代码本身没动，改的两处都是 compose 配置。

**踩到的两个环境问题**

1. **宿主机 5432 已被另一个项目的 postgres 占用**（`enterprise-visitor-demo-postgres-1`）。
   没有去动那个容器 —— 停别人的库来给自己让路，下次对方启动时又会撞回来。
   改成本项目的开发库映射到 **5433**，端口在 `docker-compose.dev.yml` 里做成
   `${DB_PORT:-5432}`，默认值不变，由 `.env` 里的 `DB_PORT` 覆盖。
   docker compose 本来就会读项目根目录的 `.env`，不用额外传参。

2. **`docker compose up` 报 `project name must not be empty`**。
   compose 默认拿目录名当项目名，而「共享知识库」全是中文字符，
   过滤掉非法字符后剩下空串。给两个 compose 文件都显式写上 `name:`
   （开发 `ideahub-dev` / 生产 `ideahub`）。
   **这条对上 VPS 同样有效** —— 只要仓库目录名还是中文，生产环境会撞一模一样的错。

**动了哪些文件**

```
docker-compose.dev.yml   加 name: ideahub-dev；端口改 ${DB_PORT:-5432}
docker-compose.yml       加 name: ideahub
.env                     新建（本机用，已被 .gitignore 忽略）
```

**怎么验证**

全部实测通过：

| 步骤 | 结果 |
|---|---|
| `docker compose -f docker-compose.dev.yml up -d` | 容器 `ideahub-dev-db-1` 起来 |
| `npm install` | 14 个包，0 漏洞 |
| `npm run db:init` | 5 张表 |
| `npm run db:seed` | 用户 96 / 灵感池 9 / 正式库 6 / 讨论 93 |
| `npm start` | 驱动 pg，`/api/health` 返回 `db:up` |
| `npm run test:api` | **通过 58   失败 0** |
| 浏览器实开三个页面 | 灵感池 9 卡 / 正式库 6 行 / 统计看板，**控制台零报错** |

统计看板数字与文档一致：累计 26、待评审 9、已采纳 6、采纳率 43%，
「其他」分类为 0 也照常出现在图上（T15 修的那个 bug 没有回归）。

**没跑的**

`npm run test:ui` —— 需要 `npm i -D playwright sharp` 再下载 Chromium，
按 README 第 148 行操作即可。UI 比对不影响功能验证，本次跳过。

**下一步**

你本机验收。确认没问题后再谈上 VPS 的事 —— 上线前至少要先 `git init`。

---

## T22 关掉 API 浮层 + 账号登录注册 ✅

**时间**：2026-08-20

### 1. 左下角的 API/SQL 浮层默认关掉了

那层黑框是当初做演示用的 —— 给非技术同事讲「点一下投票，后端到底干了什么」。
但日常用灵感库的人不需要看一片滚动的 SQL，那只会让人以为系统出问题了。

没有删掉，改成默认关闭 + 三种打开方式：网址加 `?apilog=1`、
控制台执行 `IDEAHUB_APILOG(true)`（记进 localStorage）、或设 `window.IDEAHUB_SHOW_APILOG = true`。

### 2. 真正的用户名密码登录

原来是从 `X-User-Id` 头认人，没有密码、没有会话 —— 任何人发个头就是管理员。
当初把认人这件事收在 `lib/auth.mjs` 一个函数里，这次换掉时**业务代码一行没动**，
13 个接口原样跑通。

**三个产品决定（你定的）**：用户名登录 / 开放注册 / 第一个注册的人自动是管理员。

**实现要点**

- **密码用 scrypt**，Node 内置，不加依赖。参数一起存进哈希串，
  以后调高强度时老密码仍能用自己的参数验证，不会让所有人一起登不进来。
- **会话存数据库不存签名 cookie**。因为要能「踢下线」：同事离职或密码泄露时删一行就立刻失效，
  签名 cookie 签发出去就收不回来了。
- **登录失败不区分「没这个人」和「密码错了」**，两条路径都跑一次哈希验证，
  耗时也接近。否则光看响应快慢就能枚举出公司里有哪些账号。
- **全局登录闸门**放在 `index.mjs` 的请求入口，而不是只靠每个路由自己判断。
  以后谁新加接口忘了写权限，默认是「进不来」而不是「谁都能进」。安全的默认值要往严的方向倒。
- **最后一个管理员不能降级**。降完就没人能再任命管理员，系统会锁死在
  只能上服务器改数据库才能救的状态。前后端都拦。
- **CORS 收紧了**。原来无脑回显 Origin，配上带 cookie 的接口，
  等于允许任何网站拿着同事的登录态调我们的接口。现在只放本机开发端口。

### 3. 顺手修掉的三个真问题

1. **`[hidden]` 失效**。`.authform{display:flex}` 和 `.menuitem{display:block}`
   会盖掉浏览器对 `[hidden]` 的默认 `display:none`。
   在登录页表现为两个表单同时显示（看得见），在头像菜单里表现为
   **「用户管理」对普通成员也可见** —— 那就不只是样式问题了。
2. **`db:seed` 会删光真实账号**。它原本 `TRUNCATE ... users CASCADE`，
   加了登录之后，谁重灌一次演示数据全公司就都登不进来，
   连 `sessions` 也被级联清空、正在用的人当场掉线。
   改成只删「没有密码」的那些人，也就是种子造出来的虚拟同事。
3. **`scripts/cron.mjs` 会静默失效**。它往 `/api/maintenance/*` 发 HTTP 请求，
   加了闸门后稳定拿 401，而它 catch 住错误只打一行日志。改成直连数据库 ——
   定时任务本来也不需要「以某个人的身份」做事。

### 4. 测试从 58 项涨到 89 项

新增 31 项，覆盖：未登录被拦、注册、重名（大小写不敏感）、弱密码、
用户名格式、登录成败、退出后会话立刻失效、第一个账号是管理员而第二个不是、
普通成员被 403、提权后立刻生效、降级后立刻失去权限、最后一个管理员不能降级。

**登录注册这部分是走真实 cookie 测的，没用后门** —— 认证要是也用后门测，就等于没测。

期间发现测试自己写死了 `x-user-id: 1`，而 seed 不再重置 id 序列（会和真实账号的 id 撞车），
一并改成按姓名查 id。

**动了哪些文件**

```
server/src/lib/password.mjs    新增：scrypt 哈希与强度校验
server/src/lib/session.mjs     新增：会话与 cookie
server/src/lib/auth.mjs        重写：cookie 认人，开发后门加双重开关
server/src/routes/auth.mjs     新增：注册/登录/退出/改密码/用户管理
server/src/index.mjs           全局登录闸门、CORS 收紧、后门警告
server/src/schema.sql          users 加账号字段、新增 sessions 表（可增量升级）
server/src/seed.mjs            不再删真实账号
web/login.html / login.css     新增：登录注册页
web/src/login.js               新增：登录注册逻辑
web/account.css                新增：头像菜单、用户管理样式
web/src/views/account.js       新增：菜单/退出/改密码/用户管理
web/src/api.js                 带 cookie、401 自动跳登录页
web/src/apilog.js              浮层默认关闭
web/src/main.js / mock.js      接线与演示模式兜底
web/index.html                 头像菜单 + 两个弹窗
scripts/cron.mjs               改直连数据库
scripts/test-api.mjs           58 → 89 项
.env / .env.example / README.md
```

**怎么验证**

```bash
npm run db:seed && npm run test:api     # 通过 89   失败 0
```

浏览器实测走通：注册（第一个账号拿到管理员）→ 用户管理 → 退出 →
错密码被拒 → 正确密码登回 → 重灌种子数据后登录态仍在。cookie 确认 JS 读不到。

**没跑的**

`npm run test:ui`（要额外装 playwright + sharp）。登录页是新增屏，
不在原有 5 屏基准里；头像菜单是点开才出现的浮层，不影响那 5 屏的比对。

### ⚠️ 上 VPS 前必须做的两件事

1. **删掉 `.env` 里的 `ALLOW_HEADER_AUTH=1`**。开着等于谁都能当管理员。
2. **部署完立刻自己去注册**。开放注册 + 第一个是管理员，
   意味着网址被别人先打开注册走，管理员就是他。

**下一步**

你本机验收登录注册。之后要不要做「正式库进度更新界面」和「灵感编辑界面」
（这两个接口都已有，只缺前端入口）。

---

## 2026-08-24：工作台 UI 与移动端交互升级

- 桌面端改为 6 组常驻侧边导航，14 个功能页可直接切换；顶栏同步显示当前业务分组和页面。
- 手机端改为单行顶栏 + 滑入式功能导航，保留全局搜索、页面主操作、消息和账号入口。
- 全局「提交灵感」改为跟随页面的主操作，例如「新增客户」「新增作品」「提交工作」。
- 弹窗或抽屉打开时自动让出聊天入口，Toast 按场景移动，避免挡住「保存」「否决」「转正式」等关键按钮。
- 手机统计卡改为两列，表格、客户详情、全局搜索和导航均无横向溢出。
- 补齐无后端演示模式的需求、作品、客户、案例、工作提交、漏斗、搜索和统计数据，不再出现原始接口报错。

**验收**

- `node --check web/src/main.js` 通过。
- `node --check web/src/mock.js` 通过。
- `node scripts/build-web.mjs` 通过。
- Headless Chromium 逐页验证 14 个功能页；客户详情 10 个信息卡、统计总览 7 项、移动端跨模块搜索和「新增客户」弹窗通过。
- 桌面 1440px 与手机 390px 均无水平溢出。
- `docker compose up -d --build` 已部署，`/api/health` 返回 `ok: true` 且 API / DB 均为 healthy。
- 相邻服务、端口和 Caddy 配置指纹未改动。

---

## 2026-08-24：内部业务页重设计

这一轮保留上一轮的左侧导航和移动抽屉，重做导航之内的业务页。原因是九个业务模块都被同一个渲染器压成了小字号表格，需求、内容、客户和案例缺少各自的阅读重点。

- 全站中性色改为雾灰白，主色改为低饱和靛蓝，中文字体栈改用 Noto Sans SC / HarmonyOS Sans SC / MiSans / 系统字体递降。
- 用户需求改为洞察卡，突出用户原话、真正目标和发生场景。
- 真人作品、矩阵作品和直播改为内容封面 + 指标卡，自己与对标账号仍可切换。
- 销售转化与后端交付改为方法卡、话术卡和流程卡。
- 客户档案改为客户经营卡，突出等级、阶段、当前判断和下一步；360° 详情页重组了九个信息区，并兼容两种成交字段格式。
- 案例库改为「初始问题 → 判断结论 → 策略动作」的复盘卡；工作提交改为审核收件箱卡。
- 标签管理改为 2×2 分类配置卡；数据漏斗新增最大损耗提示和六层经营卡。
- 灵感池的高级标签和来源默认收起，筛选器不再占满首屏。
- 原有增删改、附件、资料关联、账号台账和全局搜索逻辑保持不变。

**验收**

- `node --check` 通过 `main.js`、`board.js`、`client.js`、`funnel.js` 和 `tagadmin.js`。
- Headless Chromium 验证 14 个功能页和 31 个子标签状态，无页面脚本异常。
- 需求编辑、案例新增、筛选展开、客户详情及成交信息实际点击通过。
- 桌面 1440px 和手机 390px 逐页无水平溢出。
- 线上资源版本 `mt6uy8xb` 已实际打开；登录跳转和手机版均无脚本异常。
- API / DB healthy，上线前后相邻服务和 Caddy 配置指纹一致。

---

## 2026-08-24：智能导入第一版

- 在顶栏全局搜索旁新增「智能导入」入口，桌面显示文字按钮，手机保留独立图标入口。
- 支持直接粘贴聊天记录、客户反馈、会议笔记、作品复盘或工作总结，并填写原始链接。
- 新增登录保护的 `/api/import/analyze`：服务端调用 OpenAI Responses API，以严格 JSON Schema 返回结构化草稿；密钥只保存在服务端 `.env`。
- 新增 `/api/import/commit`：用户预览并修改后，一次事务写入所选内容；支持灵感池、用户需求、真人/矩阵作品、直播、销售转化、客户档案、后端交付、案例库和工作提交。
- 明确禁止智能导入绕过评审直接写正式库；灵感固定先进灵感池。
- 预览支持修改导入板块、标题、各模块专属字段和标签，也能取消任意一条。
- 使用内容指纹为带来源字段的记录提供幂等写入；客户、规则和工作提交也按业务关键字段避免重复。
- AI 暂不可用时自动回退到基础规则识别，并在界面明确标注，不让入口整体失效。
- `.env.example`、`README.md` 和 Compose 服务端环境白名单已补齐 AI 配置说明。

**验收**

- `node --check` 通过新路由、导入视图、主入口、API 与 mock 文件，前端打包通过。
- 真实数据库中一次事务成功写入 idea / demand / work / playbook / client / case / report 七类测试记录，随后均按精确 ID 清理。
- OpenAI 连接已实际请求；当前 Platform 账户返回计费未激活，因此线上会自动使用规则识别，账户启用额度后无需改代码即可切回 AI。
- Headless Chromium 验证输入、识别、修改字段、取消选择、确认导入和完成态；桌面 1440px、手机 390px 均无水平溢出，底部确认按钮始终可见。
- 线上资源版本 `mt6wftah` 已实际打开；未登录不能调用导入接口并正常跳转登录页。
- API / DB healthy，上线前后相邻服务和 Caddy 配置指纹一致。

---

## 2026-08-24：灵感池卡片展示重设计

本轮只调整灵感池，不改其它已经确认的业务页面。

- 原四列紧凑小卡改为桌面双列、手机单列的社区提案卡，给标题与正文足够阅读空间。
- 新增热榜序号，并重新分层分类、评审状态、摘要、标签、作者、热度、讨论数和支持数。
- 分类色改成顶部细色条与浅色光晕，主体保持白底与深色标题，降低生硬和拥挤感。
- 投票按钮增加「支持」语义及即时反馈；点击投票不会误开详情，详情抽屉和原筛选逻辑保持不变。
- 修正演示模式下乐观投票与 mock 数据共用对象导致票数立即复原的问题。

**验收**

- `node --check web/src/views/pool.js`、`web/src/mock.js` 与打包产物通过。
- 桌面 1440px 显示 2 列、手机 390px 显示 1 列，9 张灵感卡完整且无水平溢出。
- 投票、分类筛选、高级筛选展开和详情抽屉实际点击通过，无页面脚本异常。
- 线上资源版本 `mt6vcusi` 已实际打开；未登录访问会正常跳转登录页。
- API / DB healthy，上线前后相邻服务和 Caddy 配置指纹一致。

---

## 2026-08-24：智能导入支持页面配置 AI 模型

- 在智能导入原文区域上方新增模型状态卡和可展开配置面板；管理员可填写 OpenAI 兼容 API 地址和 API Key，实时拉取当前 Key 的可用模型并手动选择。
- API 地址只填站点域名时会自动补全 `/v1`；更换服务商地址时必须同时提供新平台密钥，防止把已有密钥误发到另一服务。
- 新增 `/api/import/provider` 和 `/api/import/provider/models`；修改权限仅限管理员，普通成员只能看到当前模型名并使用配置。
- API Key 仅经同源请求提交给后端，不写浏览器存储、不回显；使用 AES-256-GCM 加密后保存在 IdeaHub 独立 Docker 持久卷中。
- 智能识别改为优先读取页面保存的供应商配置，未保存时继续使用 `.env` 环境配置；连接失败时直接展示具体原因并保留基础规则兜底。
- 新增 `server/src/lib/ai-provider.mjs`，并同步更新 Compose、`.env.example`、README、真实 API 与 mock 演示接口。

**验收**

- 地址规范化、加密写盘及解密回读测试通过，持久化文件中不存在 API Key 明文。
- 管理员配置读取返回 200，普通成员写配置返回 403，错误密钥返回 400 和明确说明。
- Headless Chromium 验证地址、密钥、拉取模型、选择模型的完整交互；桌面 1440px 与手机 390px 均无横向溢出。
- 线上资源版本 `mt6x8omx` 已部署；`/api/health` 正常，未登录配置接口返回 401。
- API / DB healthy，上线前后相邻服务、端口和 Caddy 配置指纹一致。

### 后续：模型接口错误诊断增强

- 将原先合并显示的 401 / 403 提示拆开，直接透出供应商返回的安全错误信息和请求 ID，便于判断令牌、分组、额度或 IP 限制。
- API Key 输入会自动移除网页复制时可能夹带的零宽字符，并会主动识别带 `*` 的脱敏 Key。
- 使用无效诊断令牌验证苍原 `/v1/models` 确实返回 401 `Invalid token`，错误状态已能在页面准确显示；线上 API healthy，相邻服务无变化。

### 后续：苍原算力一键配置

- 当系统尚未保存自定义供应商、旧环境仍指向 OpenAI 官方地址时，配置面板自动预填苍原算力地址。
- 管理员现在只需输入苍原算力 API Key、拉取模型、选择模型并点击「使用此模型」；接口会自动补全 `/v1`。
- 更换供应商时不会复用原供应商密钥，避免把已有密钥发送到不同服务商。
- 已保存过的自定义供应商配置继续保持原样，不会被默认值覆盖。

---

## 2026-08-24：第一版任务 15 / 16（简单统计与智能搜索）

### 任务 15：统计页只保留关键经营数字

- 统计页精简为灵感池、用户需求、正式内容、客户和案例五个关键数量，不再把作品、资料关联和灵感内部过程指标混在同一层。
- 五类数量统一只统计未删除记录；灵感池为待评审与评审中，正式内容为已采纳，名称、口径和跳转目标保持一致。
- 新增基础销售漏斗：有效客资、已加微信、已咨询、陪跑成交、续费/转介绍。后一阶段累计计入前一阶段，已流失客户排除。
- 每个关键数量都能跳回对应资料模块；每层漏斗都携带真实客户阶段条件，点击后打开客户档案并显示当前筛选，可一键恢复查看全部客户。

### 任务 16：用户主动触发的 AI 语义搜索

- 输入框仍默认使用即时关键词搜索；只有点击「智能」或关键词无结果后主动选择时才调用模型，避免无意消耗额度。
- 模型只接收用户当前搜索词，不发送客户档案或资料正文；服务端生成少量近义检索词，再由本地数据库完成检索和排序。
- 针对兼容网关优先使用 Chat Completions 与低推理强度，不支持时再尝试 Responses API；35 秒内模型不可用、超时或输出无效时自动降级为关键词搜索并给出中文提示。
- AI 输出会保留完整表达，同时抽取去掉「客户 / 用户 / 对方」等对象称呼的核心词，避免「客户断联」搜不到库中「断联」。
- 结果区显示智能理解词；非原关键词命中的记录明确标记「语义相关 · 命中词」，原关键词结果仍排在前面。

### 上线验收

- 12 个受影响的 JS / MJS 文件通过 `node --check`，前端生产打包通过。
- 真实 API 与数据库逐项核对：五类数量一致、五层漏斗一致且单调递减，库中已有软删除记录均未进入统计。
- 真实模型查询「客户一直不回复」在约 5 秒内扩展出「断联 / 失联 / 停止沟通」等表达，并命中 2 条包含「断联」的资料；无模型配置时关键词降级契约通过。
- 公开 HTTPS 页面完成只读浏览器验收：五张统计卡、五层漏斗、漏斗跳转后的 API 数量与页面数量一致，智能结果标识正确；桌面 1440px 与手机 390px 均无横向溢出，浏览器控制台无错误。
- 生产资源 `mt718vet` 已部署，IdeaHub API / DB healthy；上线前后相邻服务、端口和共享 Caddy 配置指纹一致。
- 生产库已有真实业务数据，因此没有运行会写入数据且依赖种子条数的旧版全量 `test:api` / CRUD E2E；本次使用真实数据上的只读统计、搜索与浏览器契约回归，未新增、修改或删除业务记录。

---

## 2026-08-24：全站 UI 收口与今日工作台

- 新增「今日工作台」并设为登录后的默认入口：聚合待我审核、评审中灵感、服务中客户、用户需求、优先事项、客户转化和团队资产；提供 AI 整理、记录灵感和新增客户快捷入口。
- 品牌定位从「公司内部灵感库」统一为「经营协作台」，登录页、主站图标和副标题使用同一套视觉语言。
- 收敛顶栏与侧栏：语义搜索和 AI 整理职责分开；侧栏当前项改为克制的浅色定位，不再使用高饱和渐变阴影。
- 业务概览从重复总数改成行动指标，例如待补原话、待完成初筛、高价值待跟进、缺少报告、待审核和团队卡点。
- 灵感卡去掉与热榜编号、支持数重复的热度条和右上纯装饰色块；客户等业务卡的删除操作统一收进「更多」菜单。
- 数据漏斗改为逐步转化卡：每一步只和上一步比较，同时展示数量、转化率与流失量，最大损耗单独标记。
- 桌面侧栏断点从 1025px 调整到 1181px，消除 1024 / 1025px 的布局断崖；手机搜索增加聚焦遮罩，通知收进头像菜单，聊天与提示条互相避让。

**涉及文件**

- `web/index.html`、`web/login.html`、`web/login.css`、`web/styles.css`
- `web/src/main.js`、`web/src/views/dashboard.js`、`web/src/views/board.js`
- `web/src/views/pool.js`、`web/src/views/funnel.js`

**验收**

- 全部前端 JS 通过 `node --check`，`node scripts/build-web.mjs` 生产打包通过。
- Headless Chromium 逐页切换 15 个一级页面通过；今日工作台快捷操作、卡片更多菜单、八步漏斗与手机搜索交互通过。
- 1440px、1025px、390px 三档均无水平溢出；1025px 使用抽屉导航，手机不再常驻红色删除按钮，页面运行无脚本异常。
- 容器内 `/api/health` 与数据库连通；旧版 `test:api` 依赖开发用 header auth 和固定种子数据，生产已关闭该后门，因此不以它作为本次上线判断。
- 生产资源 `mt7bjovr` 已部署；HTTP / HTTPS 健康检查均为 200，线上桌面与手机浏览器复核通过，部署前后邻居服务、端口和共享 Caddy 指纹完全一致。

---

## 2026-08-24：Studio V2 全站视觉重构

- 新增独立 `web/studio.css` 视觉层，不改业务数据、权限和接口；品牌从通用紫色 SaaS 后台重构为温暖、成熟的「内容与经营研究室」。
- 全站采用暖纸色、深墨、梅子色、陶土与鼠尾草语义色，统一字体层级、线性分隔、低阴影和小圆角，减少无业务意义的卡片、药丸与渐变。
- 桌面端改为深色研究室侧栏与编辑部式内容区；顶部品牌区固定为 230px，不再被页面标题或主操作挤压。
- 今日工作台改成每日编辑台；灵感池改为编辑提案清单；用户需求改为研究摘录；真人 / 矩阵作品改为内容档案；客户改为横向客户卷宗；案例改为判断与行动推理链；销售、交付、统计和漏斗统一成经营台账。
- 登录页改为双栏研究室入口，弹窗、抽屉、智能导入、聊天和系统配置同步换肤；移动端重排快捷动作和业务卡，并修复首页快捷入口造成的横向溢出。
- `web/index.html` 与 `web/login.html` 使用 `data-ui="studio"` 和独立缓存版本 `studio3`，可与历史样式清晰隔离。

**验收**

- 全部前端 JS 通过 `node --check`，生产打包通过；15 个一级页面在 1440px 与 390px 逐页切换无水平溢出、无脚本异常。
- 新增、AI 整理、灵感详情、客户详情、移动导航和移动搜索等共 36 项浏览器检查全部通过。
- 线上桌面工作台、手机灵感池和登录页实际加载 `studio.css?v=20260824-studio3`；最终生产资源 `mt7dt41y` 已部署。
- HTTP / HTTPS `/api/health` 均为 200，API 与 PostgreSQL healthy；未新增、修改或删除业务数据。
- 部署前后相邻服务、端口和共享 Caddy 配置指纹完全一致。

---

## 2026-08-24：Soft UI 柔和简明版

- 新增独立 `web/soft.css`，并将主站和登录页切换到 `data-ui="soft"`；上一版 Studio 样式保留但不再参与渲染，方便安全回退。
- 去掉深色侧栏、锐利长分隔线和偏正式的衬线标题，改为浅暖灰背景、低饱和梅子 / 陶土 / 鼠尾草配色和统一无衬线层级。
- 今日工作台改为柔和欢迎区、轻量行动卡和低阴影经营模块；灵感、需求、作品、客户、案例、交付与数据保留各自业务结构，同时统一圆角、留白和信息密度。
- 顶部搜索、筛选器、标签、表格、登录页、普通新增弹窗、AI 智能导入、详情抽屉、聊天和移动侧栏全部同步换肤。
- 加入克制的悬浮抬升、按钮反馈、页面淡入和焦点状态；系统开启“减少动态效果”时会自动关闭动画。
- 修复柔和版首轮预览中桌面侧栏和移动抽屉受顶栏模糊上下文影响的问题，并修正内容封面和客户下一步信息的可读性。

**验收**

- 1440px 桌面端完成 37 项页面与交互检查；1024px 平板和 390px 手机完成 33 项断点检查，全部通过且无横向溢出。
- 15 个一级模块、AI 整理、新增表单、灵感详情、客户详情、卡片悬浮、移动导航、移动搜索和登录页均通过真实浏览器验证，无页面脚本异常。
- 线上已实际加载 `soft.css?v=20260824-soft2`，最终生产资源为 `mt7fe27k`；桌面工作台、手机灵感池与登录页复核通过。
- HTTP / HTTPS `/api/health` 均为 200，API 与 PostgreSQL healthy；未新增、修改或删除业务数据。
- 部署前后相邻服务、端口和共享 Caddy 配置指纹完全一致。

---

## 2026-08-24：灵感池改为焦点灵感画布

- 放弃横向资料清单式布局，改为「一条焦点灵感 + 双列灵感画布」：当前排序下的第一条内容跨列突出，其余灵感以双列轻卡浏览。
- 卡片内部重新安排阅读顺序：热度 / 分类 / 状态在顶部轻提示，标题与正文成为主体，标签居中承接，作者、评论与支持统一收到底部。
- 焦点灵感使用更大的标题、完整正文空间和独立作者互动区；普通灵感保持相同信息结构，减少视觉跳跃。
- 1024px 平板保留双列画布，390px 手机自动变成单列信息流；空状态跨满内容区。
- 灵感池说明更新为「先浏览、再共创」，与新的浏览式呈现保持一致。

**验收**

- 桌面、平板和手机均验证 9 张灵感完整渲染且无横向溢出；桌面焦点卡跨列、手机焦点卡正常回落单列。
- 最新 / 热度排序重排、卡片悬浮、支持计数和详情抽屉实际点击通过，浏览器无页面脚本异常。
- 线上已加载 `soft.css?v=20260824-soft3`，最终生产资源为 `mt7fmly4`；HTTP / HTTPS API 与 PostgreSQL healthy。
- 部署前后相邻服务、端口和共享 Caddy 配置指纹完全一致，未改动业务数据。

---

## 2026-08-25：技术1 的采集分析 JSON 可以直接推成对标作品

**要解决的问题**

原来技术1 只能通过 `POST /api/ingest/save` 推对标作品，而那个口子吃的是
`title / url / metrics / pillar` 几个扁平字段。技术1 实际跑出来的是一份
schema_version 13 的完整分析 JSON（约 30KB）：带时间码的逐字稿、AI 的视频七问和
评论五问（每条带原话证据）、高赞评论原文、对标账号的粉丝数与简介、公开互动数。
这些一个都塞不进去 —— 结果业务人员在 IdeaHub 里只看到一个标题和一个链接，
想看拆解还得回技术1 那边翻，这趟对接等于白做。

**改了什么**

- 新增 `POST /api/ingest/analysis?channel=persona|matrix|live`：**整份 JSON 原样收下**，
  技术1 不用改导出格式、不用拆字段、不用转 CSV。`channel` 写在 URL 上，
  支持 `persona,matrix` 一次进两个板块。认证仍走 tech1 的 API key。
- 一次请求做三件事：写对标作品台账（works）、**自动给对标账号建档**（channel_accounts，
  粉丝数每次刷新）、整份 JSON 存进新表 work_analyses。
- 幂等键 `source_ref = t1:<task_id>:<channel>`。带 channel 是因为 works 的唯一索引
  只认 (source_type, source_ref) 不带 channel —— 同一个视频可能同时是真人和矩阵的对标。
- 新表 `work_analyses`（一对一挂 works）。**不塞进 works**：/api/works 是列表接口，
  一屏几十条，每条捎一份逐字稿就是几百 KB。列表只带一小块 digest（863 字节），
  点开某条才拉完整分析（`GET /api/works/:id/analysis`）。
- `payload` 列刻意用 `JSON` 而不是 `JSONB`：jsonb 会重排对象键，
  而 `ai_analysis.items` 的**键顺序就是七问的阅读顺序**（「这条主要讲什么」在最前）。
  另外在 `t1-analysis.mjs` 里留了一份 AI_ORDER 名单做第二道保险。
- **封面图收到时就下到本地**（`data/uploads/bench-covers/`，走 `GET /api/works/:id/cover`）。
  平台的封面地址带时间签名（路径里就写着 `/202608251136/`），几天后 404；
  而且它是 http:// 的，HTTPS 页面上会被当混合内容拦掉。下载失败不影响入库。
- 前端：对标作品有分析时换成专用卡片（封面 + 账号粉丝数 + 赞/藏/评 + 一句话主题 + 话题），
  点开是**只读**拆解抽屉（8 个区：封面与识别置信度 / 对标账号 / 互动 / 话题 /
  AI 视频拆解 / AI 评论洞察带原话 / 高赞评论 / 逐字稿 / 数据出处）。
  要改台账字段走卡片右上角「更多 → 编辑台账字段」。
- 对标列表排序退一步用 `coalesce(published_at, received_at)`：技术1 抓不到发布日期，
  只按 published_at 排会把新采集的全挤到最下面。
- `推送到ideahub.py` 加 `analysis` 命令（可直接推一个文件夹）；
  `docs/接入说明.md` 和 `web/接入说明.html` 同步补了这一节。

**验收**

- 用 `女孩子无聊的本质是生命力的匮乏.json`（小红书，4分56秒，3693 字逐字稿，扫了 107 条评论）
  实测：推 persona 建出 1 条 + 自动建对标账号「可可拆爆款 12.3万粉」；
  重推同一份 `created:false` 不产生副本；`?channel=persona,matrix` 一次进两个板块。
- 互动数缩写解析正确：赞「4万」→ 40000、藏「3.2万」→ 32000、评「647」→ 647。
- 列表接口整个响应 4KB（digest 863 字节/条），详情接口 35KB，封面接口 27KB image/jpeg；
  未登录取详情和封面均 401。
- 七问顺序修复前后对比：改 JSON 列类型 + AI_ORDER 之后稳定为
  「这条主要讲什么 → 针对什么人 → 用户需求 → 内容怎么展开 → 解决办法 → 值得参考 → 还能延伸」。
- 浏览器实测（1440×1000）：对标卡片与抽屉 8 个区全部渲染，封面从本地取到 1080×1441，
  无页面脚本异常。
- 生产资源 `dist/app.js?v=mt8aw6hl`；HTTP `/api/health` 200，API 与 PostgreSQL healthy。
- 部署前后相邻服务、端口和共享 Caddy 配置指纹完全一致（check-neighbors.sh 无差异）。
- 联调用的临时 tech1 密钥和自测账号已删除/停用；库里留下了那条实测的对标作品作为示例。

### 后续：三份数据字典补齐（含一次意外的漂移修正）

`docs/schema/` 那三份是给要接进来的人看的结构说明，之前只补了接入说明、没动它们，这次补齐。

- `ideahub-schema.sql` 用 `pg_dump -s --no-owner --no-privileges` 从线上库重出，不是手改的。
- `数据字典.md` / `数据字典.html` 补上 `work_analyses` 一节（表一览 + 逐表字段 + 列数 +
  「内容运营」组的表数），并把首页「26 张表」改成 27。
- **顺带发现一处漂移**：这三份是 8/24 03:20 生成的，之后加的软删除列
  （`cases` / `channel_accounts` / `clients` / `playbook_items` / `works` 的 `deleted_at`，
  以及五个 `idx_*_alive` 部分索引）一直没进字典。这次一并补了 ——
  字典写着「和线上库一定一致」，就不能让它悄悄不一致。
- 校验方式不是肉眼看：写了个脚本把两份文档里**每张表的字段名、顺序、类型、可空**
  逐列和线上 `information_schema` 对，27 张表 260 列全部一致才算过。
- `数据字典.html` 用无头浏览器渲染确认：27 张卡片、分组计数 5+3+2+2+1+14=27、
  work_analyses 卡片 8 行字段齐、无横向溢出、无脚本异常。
- 这三份文件不在 web 构建产物里（是独立文档），所以本次**没有重新部署**，线上未受影响。

### 后续：封面可以直接把原图塞进 JSON（解决对标封面糊的问题）

上线后同事一眼看出封面糊。查下来不是显示问题，是**源头**：

- 技术1 给的 `thumbnail_url` 是小红书 CDN 的**预览变体** `!nd_prv_wlteh_jpg_3` ——
  尺寸写着 1080×1441，实际只有 27KB，**每像素 0.017 字节**（正常清晰度 0.15~0.4）。
- 我们这边没有二次压缩：CDN 返回 27239 字节，落盘也是 27239，逐字节一致。
- 显示端也没拉伸：卡片按 196px 宽显示一张 1080 宽的图，是缩小不是放大。
- **换清晰度变体救不回来**：同一地址试了原图 / webp / 大图等 6 种后缀，**全部 403** ——
  那个签名只对 `prv` 这一个变体有效。

所以只能从源头解决 —— 让技术1 把整张封面直接放进 JSON：

- `media_assets.video.cover_image_b64`，`data:image/jpeg;base64,xxx` 和裸 base64 都收。
- **按真实字节认格式**（看文件头魔数），不信声明的 mime ——
  声明成 jpeg 实际是一段 HTML 或者截断的半张图，存下来就是卡片上一个碎图标，最难查。
- 没给这个字段就**自动退回去下 `thumbnail_url`**，行为和以前完全一样，老数据和老推送不受影响。
- 请求体上限这个口子单独放宽到 8MB（其他接口仍是 1MB），`readJson` 加了个 max 参数。
- **图落地后从存档 payload 里摘掉**，只留一句说明。否则同样几百 KB 存两遍，
  而且 `/api/works/:id/analysis` 每次都会把它整个吐出来 —— 详情从 20KB 变成 800KB，
  只为一张页面上根本不从这里取的图。
- `digest.coverFrom` 记来源（`inline` / `url`），排查时一眼看得出用的是哪条路。

**验收**

- 造了一张 1080×1920 / 302KB 的高清测试封面（每像素 0.1455 字节，是平台预览图的 8.3 倍），
  塞进 JSON 推送：请求体 417KB，落地文件 301765 字节，`coverFrom=inline`，
  封面接口发出来的图与源文件**逐字节一致**。
- 存档 payload 仍是 14137 字符（base64 那 402379 字符已摘掉），
  `/api/works/174/analysis` 仍是 20800 字节 —— 没有被封面撑大。
- 界面实测（2 倍像素密度截图）：1px 细线仍是清晰的线、34px 小字可读，没有糊成灰块。
- **回退路径实测**：把不带 base64 的原始 JSON 再推一次，`coverFrom` 变回 `url`，
  封面还原成 27239 字节，`created:false`（是更新不是新建）。
- 部署前后 check-neighbors.sh 无差异；`/api/health` 200；生产资源 `dist/app.js?v=mt8d96j9`。
- `docs/接入说明.md` 和 `web/接入说明.html` 都补了「关于封面清晰度」一节（含字段、格式、
  上限、不给会怎样）。说明页免登录可访问，渲染无异常。
- 自测密钥和自测账号已停用/删除；174/176 的封面已还原成技术1 原来给的那张。

## 2026-08-25：前端卡顿排查 + 工作提交支持传图

### 一、卡顿不是服务器压力，是一个聊天推送死循环

同事反馈界面响应慢。先量了一遍，服务器完全没压力：负载 0.13（4 核），
api 容器 CPU 2.5% / 内存 26MB，数据库 CPU 3.9% / 43MB，**所有接口本地耗时 7~18ms**。

真正的两个原因：

1. **物理距离**（改不动）。服务器在洛杉矶，ping 百度往返 151ms。
   同一个接口：本机 18ms / RTT 200ms 时 205ms / RTT 300ms 时 305ms ——
   **95% 的时间花在路上**。首屏：本机 452ms、200ms 延迟 683ms、300ms 延迟 1045ms。
2. **聊天「已读」把自己叫醒了，转成死循环**（已修）：

```
收到 chat:ping → refresh() 拉 /api/chat/peers → 面板开着就 loadMsgs()
  → 无条件 POST /api/chat/:id/read（一条未读都没有也发）
  → 服务端无条件 publish('chat:ping') → 广播给所有在线的人（含自己）
  → 回到第一步，被 120ms 防抖卡成每秒 ~2.5 次
```

最要命的是**一个人开着聊天窗口，所有人的浏览器都跟着刷** ——
实测连聊天面板都没打开的页面，静置 30 秒也发了 75 次 `/api/chat/peers`。

两头各断一次：

- 服务端 `/api/chat/:userId/read`：`UPDATE` 的 `rowCount > 0` 才 publish。
- 服务端 `/api/chat/group/:id/read`：`DO UPDATE ... WHERE last_read_id < EXCLUDED`，
  没前进就不返回行、也不 publish。（一开始写成 `RETURNING last_read_id` 再比大小，
  那是错的 —— 返回的是更新**后**的值，永远不小于新值，条件等于失效。）
- 前端 `loadMsgs`：这条会话确知没有未读就不发 `/read`。
  判据写成 `it && !(it.unread > 0)` 而不是 `!(it?.unread > 0)` ——
  列表还没拉到时 `it` 是 undefined，那种情况必须照发，否则会变成「打开了却标不上已读」。

**验收：静置页面的请求量从 150 次/分 降到 2 次/分**，SSE 流 20 秒内业务推送为 0（只剩心跳）。
服务端那一半对还没刷新页面的人也立即生效。

### 二、工作提交现在能传图，审核人直接看得到

同事说「传不了图」。实测下来 jpg/png 其实能传，真正被挡住的是**手机拍的照片**：

```
照片.jpg  201    截图.png  201
手机照片.HEIC  400 不支持这个格式        图.webp  400 不支持这个格式
```

iPhone 默认存 HEIC、很多截图工具存 WebP —— 直接拒收等于要求同事「拍完先转格式」，
那一步没人会做，结果就是干脆不传图。而且就算传上去了，界面上也只是一行文件链接，
审核人得一个个下载才知道图里是什么。

- 服务端允许的图片格式补齐：`.gif .webp .bmp .avif .heic .heif`（原来只有 png/jpg/jpeg）。
- HEIC/HEIF **收下但不当图片显示** —— 除 Safari 外浏览器渲染不了，
  界面上退化成一行文件并注明「iPhone 原图，需下载查看」，而不是给一个碎图标。
- 文件选择框的 `accept` 加上 `image/*`，手机上「从相册选」才能用。
- **附件区图片改成缩略图网格**，点开是大图灯箱（Esc 用捕获阶段拦下来，
  只关灯箱、不会把整条记录的编辑弹窗一起关掉）。
- 新建记录时选中的图**保存前就显示缩略图** —— 传错人/传错图要在这一步发现。
- 「指定审核人」本来就有（表单里的审核人下拉），这次没动。

**验收**（都在浏览器里实测）：HEIC / WebP / GIF 现在都 201；
一条提交里 2 张图渲染成网格且都加载成功（1200×800），HEIC 落到文件行并带提示；
点缩略图弹出大图、按 Esc 只关大图、编辑弹窗还在；新建时选 2 张图，缩略图预览正常；
无页面脚本异常。部署前后 check-neighbors 无差异，`/api/health` 200，
生产资源 `dist/app.js?v=mt8e9d4j`。自测账号、自测提交和附件已全部删除。

### 后续：图文笔记（image_post）的图现在能显示了

同事反馈矩阵对标里新推的两条看不到图。查下来**两个问题叠在一起**：

1. **代码漏了一整种内容形态**（主因，已修）。这两条是图文笔记 `media_type=image_post`，
   没有视频，图在**顶层 `images[]`** 里（一条 9~10 张，每张还带一段 OCR 文字）。
   而封面只从 `media_assets.video.thumbnail_url` 取 —— 图文笔记于是变成一个
   没封面、也看不到任何图的空壳，而那些图**就是它的全部内容**。
2. **地址早就过期**（技术1 那头）。路径里的 `202608241623` 是**失效时间**不是采集时间：
   技术1 昨天 16:23 前采的，同事今天 08:22 才推，推的时候图已经全部 403 了。
   （对照：`202608251136` 那条今天 07:53 还能下 —— 时间戳当失效时间解释，两条都对得上。）

改动：

- `t1-analysis.mjs` 读顶层 `images[]`（按 index 排序，index 有意义：图文笔记一页一个论点），
  封面候选退回第一张图；`analysisView` 把整组图连同 OCR 文字一起吐出来。
- `cover.mjs` 加 `mirrorImages()`：整叠图逐张下到本地，文件名带序号。顺序下不并发 ——
  一条九张，并发对那个 CDN 像一次小爆发，而这是后台入库、没人在等。
- 本地文件名存在 `digest.imageFiles`，**列表接口用 `digest - 'imageFiles'` 摘掉再下发**：
  一条十几张、二十条就是两百多个文件名，卡片上一个都用不到。
- 新接口 `GET /api/works/:id/image/:i`。序号是拿去 `digest.imageFiles` 里**查**，
  不是拿路径参数拼文件名 —— 拼的话就得自己防目录穿越，没必要冒那个险。
  封面和图文共用抽出来的 `sendLocalImage()`，免得两处慢慢长歪。
- 抽屉新增「图文内容（N 张）」一区，放在话题之后、AI 之前（先看内容，再看 AI 的解读）。
  每张图配它的 OCR 文字，点开看大图。
- 没存下来的那些**不硬塞平台原地址去试**：那是 http:// 的，HTTPS 页面上会被当混合内容拦掉，
  结果是一排碎图标加一堆控制台报错，看的人只会以为系统坏了。改成一块占位 +
  一句「图片已过期，让技术1 重推」+ 去原作品的入口。
- 卡片右下角：视频显示时长，图文显示「图文 N 张」——「这条是视频还是图文」是选对标时第一眼要判断的。
- 抽屉里 AI 那一区的标题跟着内容形态走，图文笔记不再叫「AI 视频拆解」。
- 灯箱从 `board.js` 抽成 `web/src/lightbox.js`：board 路由到 bench.open，bench 又要用灯箱，
  留在 board 里就成了循环 import。

**验收**：造了一份 4 张图的图文笔记（第 4 张故意给坏地址）推进去 ——
3 张落地、第 4 张正确缺席；详情接口 `imagesLocal: 3 / 共 4`，逐张 `local` 标记正确；
`/api/works/193/image/0..2` 返回 200 + 原始字节，`image/3` 是 404；
列表接口里确认没有 `imageFiles`；界面上 4 个图框（3 张图 + 1 块「图片已过期」占位）、
OCR 文字在图下、点开大图正常、卡片角标显示「图文 4 张」，无脚本异常。
同事那条 189 现在能看到 10 张图的占位和全部 OCR 文字（图本身已过期，要重推才有）。

**一个自己造成又修掉的事故**：测试推送复用了同事真实账号「恰芒芒不恰」，
把粉丝数 1340 覆盖成我编的 58000、主页链接改成了测试地址（账号匹配只看平台+账号名，
我的假数据用了同一个名字）。已按 189 原始 payload 还原。
教训：以后造测试数据，账号名也要用一眼看得出是假的。
部署前后 check-neighbors 无差异，`/api/health` 200，资源 `dist/app.js?v=mt8etlm4`。

### 后续：看大图能左右翻了

图能看见之后的第一个反馈：点开一张图，要退出去才能点下一张。
图文笔记一条就是九张、一页一个论点 —— 这样翻的话那条笔记根本读不下来。

`lightbox.js` 从「一次一张」改成「一次一组」：

- `openLightbox(items, index)`，同时兼容原来的 `openLightbox(url, name)` 写法。
- 左右两个圆钮 + `←` `→` 方向键；右下角「2 / 4」计数。
- **到头就停住，不绕回第一张** —— 图文笔记是有顺序的，
  从最后一张跳回第一张会让人以为自己看漏了中间几张。
  到头的那个钮变暗而不是消失：位置忽然空掉会让人以为点错了。
- **只有一张图时翻页钮和计数都收起来**，否则界面上多两个永远点不动的箭头。
- **预取相邻那张**。服务器在洛杉矶，一次往返 200ms 起，不预取的话每翻一张都要等一下。
- 方向键和 Esc 一样走捕获阶段：页面上有全局 Esc，不拦的话看完一张图会把整条记录一起关掉。
- 过期没存下来的图**不在翻页序列里**（它们本来就没有图可看），翻页时跳过而不是翻到一个空框。
- 两个调用点都接上了：对标拆解的图文组、工作提交的附件图（一条提交常常好几张截图，
  审核人同样要能直接翻）。

**验收**：4 张图（第 4 张是过期占位）的图文笔记 —— 灯箱里正好 3 张可翻；
计数 1/3 → 2/3 → 3/3；首张「上一张」禁用、末张「下一张」禁用；
末张再按 → 和首张再按 ← 都停在原地不绕回；Esc 只关灯箱、抽屉还在；
单图的工作提交里箭头和计数都不出现；无脚本异常。
测试数据、密钥、账号已全部清掉（这次账号名用了「ZZ测试账号请勿使用」，
不会再像上次那样撞上真实账号）。资源 `dist/app.js?v=mt8fanft`，邻居无差异。

## 2026-08-25：登录页加入 IdeaHub 手绘灵感册

参考 ThreeUI 速写本的纸张、手绘和知识笔记气质，重新设计登录页的宽屏品牌区，
但没有照搬原页面的新加坡插画、个人主页内容或整套重交互。

- 新增原创的 IdeaHub 开放式知识手账插画，内容是灯泡、便签、关联线和小图表，
  不含文字、地标、第三方标识；压成约 82KB WebP 后放在 `web/assets/`。
- 宽屏登录页改为 920×560 的左右结构：左侧展示品牌文案和手绘灵感册，右侧保持原有登录/注册表单。
- 插画只有缓慢的 4px 呼吸漂浮，并遵守系统“减少动态效果”设置；不加入翻页、放大镜等会抢表单注意力的交互。
- 820px 以下自动隐藏插画并切回轻量单卡片，手机登录和注册不会加载复杂布局。
- 给 `soft.css` 更新资源版本号，避免部署后浏览器继续使用旧登录样式。

**验收**：桌面 1440×900 下登录和注册均完整显示，页面宽高无溢出；手机 390×844 下
插画正确隐藏，登录/注册切换、自动聚焦和表单布局正常；插画实际加载为 1100×978；
浏览器无警告或脚本错误；`node --check web/src/login.js`、`docker compose config`、
`git diff --check` 通过；重新安装锁定依赖后 `node scripts/build-web.mjs` 打包成功（223KB）。

## 2026-08-25：技术1图文归档改为传图片本体

线上记录 190「NPD有一个藏不住的语言习惯」显示 8 张“图片已过期”。核对完整数据库快照后确认：

- 技术1正确传了 `images[]`、8 个临时地址和每张 OCR，不是前端漏字段。
- 地址里的失效时间是 `202608241555`，IdeaHub 在 `2026-08-25 09:21 UTC` 才收到，
  到达时已经全部 403，因此本地文件数是 0/8。
- payload 声明 `local_media_retained=true` 并带 `filename`，说明技术1本地有图，
  但旧协议只传文件名和临时 URL，没有传文件本体。这是两边对接契约的缺口。

修复：

- `/api/ingest/analysis` 接受 `images[].image_b64`（兼容三个直白别名），图片本体优先、临时 URL 兜底；
  第一张图也可直接作为本地封面。
- 分析接口请求体上限从 8MB 提到 32MB，单张仍限制 3MB、最多 20 张；写入数据库前摘掉 base64，
  数据库和详情接口不会被图片字符串撑大。
- 返回 `imagesExpected / imagesStored / imagesStoredNow / warnings`，图片没存全时明确告诉推送方，
  不再“HTTP 200 但静默全丢”。
- 重推按序号合并本地文件：新成功的覆盖旧图，本轮失败的保留旧图，避免重推后图片反而变少。
- `推送到ideahub.py` 会按 `filename` 在 JSON 附近的固定安全目录自动找图并以内嵌方式附入请求；
  不递归搜索、不允许借相对路径读取别处文件。在线 HTML、Markdown 接入说明同步更新。

**验收**：用本地真实 PNG 模拟技术1导出目录，推送脚本自动附图 1/1，生成合法 data URL；
服务端在 URL 明确失效的情况下仍正确解析内嵌图片并把第一张作为封面；旧 0/1 + 新 1/1 的序号合并、
当前图片数缩短时清理尾项均通过；三个服务端文件和 Python 脚本语法检查通过；生产前端打包 223KB；
`npm audit --omit=dev` 为 0 个漏洞，`docker compose config`、`git diff --check` 通过。

**现有记录恢复**：部署新版后，技术1须用最新版脚本、原 task_id `f772530c0c4f` 重推一次。
只部署代码不会凭空恢复已经失效的 8 张图片。

## 2026-08-25：台账删除改为项目内居中确认弹窗

业务板块右上角“更多 → 删除记录”原来调用浏览器原生 `confirm()`。在宽屏浏览器里它贴着页面上沿，
位置和按钮样式不可控，用户很容易没看到或没读清删除后果。

- 新增 `web/src/confirm.js` Promise 式确认组件，使用独立遮罩和中央 `alertdialog`，不和提交、评审弹窗抢同一层状态。
- 弹窗显示真实记录标题，并按板块准确区分“可恢复删除”和“不可恢复操作”；软删明确说明记录仍在数据库、可由管理员恢复。
- 使用现有 `--crit / --surface / --line / --shadow` tokens，soft 主题下是暖白卡片、柔和遮罩、克制红色确认按钮。
- 支持点击遮罩、取消按钮和 Esc 取消；打开后焦点落在“确认删除”，Tab 只在两个按钮之间循环，关闭后回到“更多操作”。
- 删除菜单在弹窗打开前先收起，取消后不会留下一个悬空菜单；确认后才真正调用删除接口。

**验收**：桌面 1440×900 下弹窗尺寸 430×257，水平和垂直中心偏差均为 0；
手机 390×844 下弹窗宽 352px，完整可见、无横向溢出；取消后记录仍在，未发送删除；
确认按钮自动聚焦，DOM 正确暴露 `role=alertdialog`、标题和说明；浏览器无脚本警告；
两个 JS 文件语法、生产前端打包（226KB）、`docker compose config`、`npm audit --omit=dev`、`git diff --check` 均通过。

## 2026-08-26：工作台“待我审核”改为专用审核视图

从今日工作台点“待我审核”会打开工作提交的通用编辑弹窗。生产记录带 7 个字段和 5 张附件时，
通用弹窗总高度超过视口，标题、顶部字段和底部保存按钮被推出屏幕；审核人还会看到提交人的内容都是输入框，
既浪费空间又容易误改（后端其实只允许审核人写反馈）。

- `#bdModal` 改为固定标题区、固定操作区、中间统一滚动；字段和附件不再各自形成互相打架的滚动层。
- 审核别人提交时启用 960px 专用双栏：左侧把标题、日期、审核人、做了什么、结果链接、问题和求助画成只读证据块，
  右侧展示附件网格并保留审核方上传入口。
- 唯一可编辑字段是“审核反馈”，底部按钮改成“提交审核反馈”；空反馈会被前端拦住并自动聚焦输入框。
- 提交人编辑自己的工作仍使用原表单，不受审核视图影响；管理员审核别人时同样进入审核模式。
- 820px 以下自动改为单栏，560px 以下证据块全部纵向排列；底部按钮始终留在视口内。
- mock 增加一条李华提交、陈屿审核、带 5 张演示附件的记录，方便以后不连生产库也能复测完整场景。

**验收**：桌面 1440×900 下审核弹窗 960×810，完整位于视口内，正文可视高 673px、实际内容高 697px，
只需轻微滚动；标题和 63px 底部操作区全程可见；5 张附件正常两列展示，DOM 中只有 `bdf_feedback` 可编辑。
手机 390×844 下弹窗宽 358px、高 760px，正文单列、底部按钮位于视口内、无横向溢出；浏览器警告为 0。
生产前端打包 233KB，两个 JS 文件语法、`docker compose config`、`npm audit --omit=dev`、`git diff --check` 均通过。

## 2026-08-26：灵感池升级为带共识反馈的提案画布

原灵感池已经有一主两列结构，但普通卡片固定高度偏大、大片留白，标题/正文/标签/作者全部落在同样的白色矩形里，
交互只有统一的上浮 3px，视觉上仍像硬拼出来的数据卡片。

研究了 Magic UI、React Bits、Motion Primitives 和 SmoothUI 的官方实现。它们都是 React/Tailwind/Motion 体系，
不适合直接安装进原生 ES Module 项目，因此只抽取可维护的交互模式，用零依赖 CSS/JS重新实现：

- Magic Card 式鼠标聚光：桌面精细指针经过卡片时，以类别色在鼠标位置形成低对比光晕；一个委托监听更新 CSS 变量，不为每张卡绑事件。
- Border Beam 式焦点边缘：仅热榜第一名使用 10 秒一圈的克制边缘流光，其他卡片保持静态，避免全屏都在动。
- Motion Primitives 式进入节奏：筛选或重排后卡片按排名每张错开 45ms 淡入；后台增量刷新不重建 DOM、不重复播放。
- 榜首新增“正在形成共识”侧栏，直接突出支持人数和讨论数；投票乐观更新会同步更新按钮、ARIA 状态和共识数字。
- 普通卡片从 302px 收紧到约 252–270px，正文两行、标签和作者区层级更紧凑；间距从 14px 提到 18px，让卡片之间有呼吸而不是靠卡片内部空白撑开。
- 手机端自动单列、隐藏榜首共识侧栏和边缘动画；`prefers-reduced-motion` 下关闭流光、错峰和过渡。

**验收**：桌面 1440×900 下 9 张卡片为 1120px 榜首 + 551px 双列，榜首高 286px，普通卡约 270px；
手机 390×844 下卡片宽 352px、单列无横向溢出，榜首共识侧栏隐藏；支持按钮实测 34→35，
`aria-pressed=false→true`，侧栏数字同步为 35；浏览器错误/警告为 0。
生产前端打包 234KB，JS 语法、`docker compose config`、`npm audit --omit=dev`、`git diff --check` 均通过。

## 2026-08-26：首页新增框架学习与详细学习中心

把 `D:\work\ideaHUB\学习内容\学习内容` 中的 14 份 PDF 内置进 IdeaHub，目标是点击后直接在网站内部阅读，
不再弹出下载。原 PPTX/DOCX 没有复制进项目，只使用用户转换后的 PDF。

- 框架学习 7 份，共 147 页；详细学习 7 份，共 162 页。14 份 PDF 都有可选文字层，首屏全部渲染正常。
- 首页快捷区从 3 个入口调整为 5 个紧凑入口，新增“框架学习 / 判断链路”和“详细学习 / 专业详解”；
  桌面每个按钮宽 112.5px，原有三个入口向左收紧后完整容纳，不换行、不溢出。
- 新增独立 `learning` 视图：顶部分类说明，左侧目录和搜索，右侧同源 PDF iframe；支持分类切换、上一份、下一份、返回工作台。
- 记录上次打开的板块与资料，下一次回来继续；隐私模式禁用 localStorage 时自动退化，不影响阅读。
- PDF 使用清理后的英文路径，界面保留中文标题；文件放在 `server/content/learning` 私有目录，
  通过必须登录的 `/api/learning/*` 白名单接口读取，不放在无需登录的 `web/` 静态目录。
- 阅读接口返回 `application/pdf`、`Content-Disposition: inline` 和 `Accept-Ranges: bytes`，支持浏览器按页分段读取；
  列表按钮只切换 iframe，不提供下载动作。
- 修复 Chromium 内置 PDF 查看器加载时把主页面滚到 iframe 的问题：打开前保存主滚动位置，加载后恢复，首次进入始终从学习中心顶部开始。
- 手机端学习中心改成单列，目录高 280px、PDF 阅读器宽约 350px；分类、搜索和前后切换仍可用。

**验收**：14/14 PDF 结构与首屏渲染通过，总计 309 页、约 11MB；本地学习接口返回 `application/pdf` + `inline`；
桌面 1440×900 下目录宽 292px、阅读器宽 814px、PDF iframe 812×648，站内正常显示；搜索“自我价值”只返回一条，
点击后切到 `self-worth-detail.pdf`，无下载；框架/详细分类分别显示 147/162 页；返回工作台正确恢复首页。
手机 390×844 下无横向溢出，PDF iframe 宽 350px；浏览器错误/警告为 0。
生产前端打包 244KB，相关 JS 语法、`docker compose config`、`npm audit --omit=dev`、`git diff --check` 均通过。

## 2026-08-26：技术2可按 externalId 上传客户附件

原有客户附件上传只接受 IdeaHub 内部客户 ID，并依赖员工登录 Cookie。技术2虽然已有 `tech2` API Key，
但无法上传分析报告，也没有独立字段保存附件在上游系统中的原始地址。

- 新增 `POST /api/ingest/client/file`，沿用技术2现有 Bearer Key，并在路由内严格校验 `tech2` scope。
- 查询参数使用 `externalId`、`name`，可选 `sourceUrl` 和 `note`；服务端通过 `clients.external_id` 找到客户，技术2无需知道内部 ID。
- 请求体继续采用单个文件原始字节，保留现有流式落盘、20MB 上限、格式白名单、随机磁盘文件名和 HTML CSP 隔离策略。
- 上传成功返回 HTTP 201，`id` 为附件/文档 ID，同时返回 `externalId`、内部 `refId`、`sourceUrl` 和文件元数据。
- 多文件约定为一文件一请求，可并行调用；单个失败可独立重试，不引入 multipart 解析器。
- `attachments` 新增可空 `source_url`；提供可重复执行的生产迁移 SQL。数据库写入失败时会删除刚落盘的文件，避免孤儿附件。
- Markdown、网页接入说明和接口自动化测试同步补齐；测试覆盖无 Key、建档、上传、externalId 关联、sourceUrl 落库、未建档 404 和清理。

**验收**：三个 JavaScript 文件语法、生产前端打包（234KB）、`docker compose config`、
`npm audit --omit=dev`（0 个漏洞）和 `git diff --check` 通过。当前 Docker Desktop 与本地 PostgreSQL 均未运行，
因此 `npm run test:api` 在连接 `localhost:5432` 时被拒，新增接口自动化用例尚待有数据库的环境执行。

**上线注意**：本次属于数据库结构变更。VPS 必须先备份数据库并执行
`server/migrations/2026-08-26-tech2-client-file-source-url.sql`，再重建 IdeaHub；只拉代码、重建容器不会自动补生产表字段。

## 2026-08-26：工作提交与审核弹窗支持放大和长内容滚动

工作提交里的“做了什么、遇到的问题、需要协助什么”和审核反馈原本显示高度固定，长内容虽然能保存，
但输入和复核时可视范围太小。现在提交与审核共用弹窗增加“放大 / 还原”按钮，放大时扩展到接近全屏；
标题和底部操作区固定，中间表单统一上下滚动，关闭后自动恢复默认大小且不会丢失已输入内容。

- 所有多行输入框支持拖动增高，并在内容超过当前高度时显示框内滚动。
- 审核反馈默认高度提高到 136px；审核专用双栏、附件区和只读证据块保持不变。
- 手机端按钮收为图标，390px 宽度下审核内容自动单栏，正文可滚动，底部提交按钮保持可见。

**验收**：JavaScript 语法、生产前端打包和 `git diff --check` 通过。桌面实测 18 行、512 字内容完整保留，
输入框 `overflow-y:auto` 且 `resize:vertical`；默认表单可视高 497px、实际内容高 797px，可上下滚动。
390×844 下审核弹窗放大后为 374×828px，中间正文可视高 675px、实际内容高 1469px，单栏且无横向溢出。

## 2026-08-26：修复手机端 PDF 只能查看第一页

手机浏览器内置 PDF 预览器在嵌入页面时兼容性不一致，部分设备只显示第一页且无法在阅读区内继续下滑。本次将学习中心的原生 PDF iframe 改为站内 PDF.js 连续阅读器：一次建立全部页的连续布局，靠近视口时再逐页渲染，手机上下滑动即可连续阅读整份资料。

- PDF 继续通过已登录保护的 `/api/learning/*` 接口读取，没有改成公开静态文件，也没有提供下载入口。
- 阅读区使用普通网页滚动和 Canvas 页面，支持手机触控惯性滚动、键盘翻页、当前页码与加载进度提示。
- 采用懒渲染：40 页资料会先建立 40 个页面位置，只渲染当前页附近的内容，避免手机一次生成全部高清画布导致卡顿或内存过高。
- PDF.js 浏览器运行文件在构建时复制到 `web/vendor/pdfjs/`，版本锁定并保留 Apache 2.0 许可证；构建后从服务端依赖中移除完整包，避免镜像无谓增大。
- 切换资料或离开学习页面会取消旧的加载与渲染任务，避免后台继续占用流量和内存。

**验收**：390×844 手机视口下打开 40 页资料，页面容器一次建立 40 页，首屏仅懒加载相邻 4 页；阅读区高度 591px、连续内容高度 17198px，证明不是单页嵌入。JavaScript 语法、生产前端打包、`docker compose config`、`npm audit --omit=dev` 与 `git diff --check` 均通过。

### 后续：手机端增加站内放大阅读

连续分页解决了“只能看第一页”，但适宽显示时专业课件的小字仍然不够清晰。阅读器现在增加站内缩放，不需要调用浏览器整页缩放：

- 工具条提供缩小、当前倍率/适宽、放大三个按钮，范围为 100%–250%，手机按钮保持 40px 点击热区。
- 支持在 PDF 区域双指缩放；双击当前内容可在适宽与 180% 之间快速切换。
- 缩放以手势或屏幕中心为锚点，尽量保留正在阅读的位置；放大后阅读区可横向移动，纵向连续翻页仍然有效。
- 缩放过程中先即时放大现有页面，手势停止后重新渲染视口附近页面，避免文字只被拉伸而发糊。
- “适宽”一键回到完整页面宽度，切换另一份资料时自动恢复适宽，避免继承上一份资料的横向位置。

**验收**：390×844 手机视口下，适宽页面 316px；按钮放大至 125% 后页面 395px，连续放大至 200% 后页面 632px、阅读区横向内容 646px；双击切换到 180% 后页面 569px。放大状态下纵向滑动从第 1 页进入第 2 页，适宽按钮可恢复 316px 且横向位置归零。浏览器无警告或脚本错误；JavaScript 语法、生产打包、依赖审计、Docker 配置和差异检查均通过。

### 后续：手机与电脑增加全屏阅读

普通学习页面还要同时容纳目录、资料标题和站点导航，手机上的实际阅读高度有限。阅读器工具条新增“全屏”入口，手机与电脑使用同一套沉浸式阅读能力：

- 点击全屏后，阅读器覆盖整个视口，站点导航、目录和其他卡片让位，只保留资料标题、页码、缩放与退出按钮。
- 浏览器支持原生 Fullscreen API 时优先进入系统全屏；不支持的 iPhone/Safari 自动使用 CSS 沉浸模式，功能不降级。
- 全屏阅读继续支持 100%–250% 缩放、双指/双击放大、左右移动和上下连续翻页；退出时保留倍率、页码和阅读位置。
- 进入和退出时重新计算适宽尺寸并重绘视口附近页面，桌面不会沿用普通内容栏的小宽度，手机也不会只是放大原来的固定框。
- 手机全屏头部压缩到单行，按钮为 42px 点击热区，并处理顶部刘海与底部安全区；电脑可按 Esc 退出。

**验收**：390×844 下全屏层为 390×844，PDF 可视区高 737px、适宽页宽 366px；200% 时页宽 732px、横向内容宽 746px，纵向滑动成功从第 1 页进入第 2 页。退出后仍保持 200%、第 2 页和原阅读位置，页面滚动锁解除。1440×900 下全屏层完整覆盖视口，PDF 可视区高 790px、适宽页宽 1080px；Esc 正常退出。手机和桌面浏览器均无警告或脚本错误。

**手机入口可见性修正**：线上构建核对确认 `8869696` 的全屏逻辑与样式均已生效，问题不是 VPS 未更新，而是手机工具栏把“全屏”文字隐藏成纯图标，窄屏时最右侧入口又容易被页码和缩放组挤压。普通手机工具栏改为两行：第一行固定显示页码与带文字的紫色“全屏”按钮，第二行居中显示缩放；进入全屏后再收为一行。320px 极窄视口下按钮宽 78px、文字完整可见、工具栏无横向溢出；全屏内“退出”按钮宽 70px，页码、缩放和退出同排且无溢出。

## 2026-08-26：全局搜索框刷新后强制清空

浏览器会在刷新或前进后退缓存恢复时忽略 `autocomplete="off"`，把上一次搜索词重新写进顶栏搜索框。项目没有使用本地存储或接口保存搜索词，但现在进一步在搜索模块初始化、`pageshow` 和浏览器延迟恢复后主动清空输入值与旧结果面板；用户已经主动聚焦输入时不执行延迟清空，正常搜索不受影响。搜索框补充 `type="search"`、独立 `name`、关闭首字母大写与拼写检查，并标记常见密码管理器不要接管。

**验收**：输入“华俊杰”后刷新，搜索框值为空且结果面板关闭；刷新后手动输入“新的搜索词”，等待超过延迟清理时间仍完整保留；再次刷新后重新为空。浏览器无警告或脚本错误，JavaScript 语法、生产前端打包与差异检查通过。

## 2026-08-26：修复作品板块“自己账号 / 对标账号”切换无反馈

标签的数据请求原本已经接好，但动效层同时把旧按钮的 `aria-selected=true` 当成选中依据。切到“对标账号”后，新按钮获得 `.on`，旧按钮却仍保留 ARIA 选中态；滑动选中块优先找到排在前面的旧按钮，因此视觉一直停在“自己账号”。生产网络还有延迟时，旧卡片继续留在屏幕上，用户看到的就是点击后毫无反应。

- 动效层改为以业务渲染器的 `.on` 为第一真值，仅在没有 `.on` 时才回退读取 ARIA 状态。
- 点击标签时立即同步 `.on`、`aria-selected` 和数量加载态，不再等待接口返回后才反馈。
- 切换板块时立即显示骨架卡片，避免旧账号内容冒充新筛选结果。
- 每个板块增加请求序号与目标标签校验；快速来回点击时，较早但较晚返回的请求不能覆盖最后一次选择。

**验收**：真人作品从“自己账号”切到“对标账号”后，ARIA 选中态、白色滑块和数据同时切换，滑块从 0px 移到 83px，数量 2→1，首条内容随之变化；切回后数量恢复为 2、滑块回到 0px。矩阵作品同样能切到对标状态；浏览器无警告或脚本错误。JavaScript 语法、生产打包和差异检查通过。

## 2026-08-26：第一轮全站即时响应优化

实测线上连接复用后的接口通常需要 213–258ms，偶发 778ms；首次 TLS/HTTPS 建连约 1.1–2.7 秒，而本地同类健康请求稳定在 1–3ms。VPS CPU 约 4%、内存约 42%，瓶颈主要是洛杉矶到用户侧的网络往返，不是 JavaScript/Node 性能或机器算力。本轮不换服务器，通过减少等待链路和先显示已有内容改善体感：

- 生产静态页面和 API 本来由同一个 Node 进程提供，启动时不再先请求 `/api/health` 再请求 `/api/me`，直接省掉一个串行跨洋 RTT；本地 5173 的后端探测与 mock 降级保持不变。
- 工作台成功加载后保存按用户隔离、最长 24 小时的轻量数据快照。刷新或再次打开时先立即绘制上次内容，再后台并行刷新 5 个接口，不再主动清空为整屏“正在整理今天的工作”。
- 后台刷新失败时保留可用快照并提示网络较慢；多次刷新使用请求序号防止旧响应覆盖新数据。
- 退出登录清除当前用户快照；同一浏览器换账号时主动删除上一位用户的工作台缓存，避免内部摘要残留。
- 动效 tokens 从 250/350/400/500ms 缩短为 170/220/260/320ms；工作台主要按钮补充 50ms 按下反馈。

**验收**：首次成功加载后刷新，在后台 mock 数据刷新完成前已出现完整工作台快照，随后静默更新，过程中不出现 `.dash-loading`；首页快捷入口、审核数和问候语完整，浏览器无警告或脚本错误。相关 JavaScript 语法与生产打包通过，前端包约 257KB。

## 2026-08-26：工作提交附件清单首开即时显示

工作提交列表原本只返回 `fileCount`，点击编辑/审核弹窗后才单独请求 `/api/reports/:id/files`。表单能立即打开，但附件区域必须额外等待一次跨洋 RTT，因此长时间停在“加载中…”。

- 工作提交列表和单条读取现在通过 PostgreSQL lateral 聚合顺带返回附件的轻量元数据：ID、名称、类型、大小、上传方、时间与站内地址；不传文件本体，列表体积只增加少量 JSON。
- 弹窗使用列表行自带的附件元数据立即渲染，真实图片仍由浏览器 `loading=lazy` 按需读取，避免一次下载全部原图。
- 前端按 `scope:id` 缓存附件清单5分钟，客户附件或旧接口数据首次加载后，再次打开同一记录也能立即显示。
- 后台仍静默请求最新清单进行校准；弹窗已关闭、切换到别的记录或 scope 不一致时，旧响应不会画到错误记录。
- 上传或删除后主动失效缓存并强制刷新，避免继续显示已变化的旧附件。

**验收**：本地待审核记录打开的同一时刻已显示5张附件卡片与文件名，`加载中…` 为 false；180ms 后后台清单返回，附件仍为5张且没有闪烁。浏览器无警告或脚本错误；前后端 JavaScript 语法、生产打包和差异检查通过。

## 2026-08-26：聊天图片改为直接预览和连续浏览

聊天原本把图片与 Word、Excel 等普通文件统一显示成文件名链接，收图的人必须逐张点开；多选发送虽然能一次选中多个文件，实际请求仍完全串行。本次把聊天图片调整为消息内直接预览，并复用现有大图灯箱。

- PNG、JPEG、GIF、WebP、BMP、AVIF 在聊天气泡中直接显示缩略图；HEIC/HEIF 因多数浏览器不能直接显示，仍安全退化为普通文件链接。
- 点击任意聊天图片后，可以在当前会话已经加载的全部图片中左右翻页，无需退出后逐张打开。
- 多选文件最多同时发送 3 个，并在附件按钮上显示发送状态；发送目标在选文件时锁定，上传期间切换会话不会把后续图片误发给别人。
- 聊天文件选择器补齐服务端已支持的图片格式，普通文档附件的打开方式保持不变。

**验收**：桌面与 390×760 手机视口下，横图和竖图缩略图均无横向溢出；手机缩略图宽 260px。点击第一张后灯箱显示 `1 / 2`，切换下一张后显示 `2 / 2` 且按钮边界状态正确。浏览器无警告或脚本错误；相关 JavaScript 语法、生产前端打包和差异检查通过。

## 2026-08-28：内容采集中心集成功能分支

将公司内部 `dataClean@2df1a6c` 通过 Git subtree 引入 `services/data-clean/`，在 `feat/content-collector` 功能分支完成采集引擎、IdeaHub代理、Docker资源控制和统一前端。此功能尚未合并 main，最终门槛是在VPS执行 Collector smoke 与真实二维码登录。

- Collector除 `/health` 外要求至少32字节内部令牌；IdeaHub只提供固定路径代理，普通成员只能访问自己创建的任务，平台登录、二维码与删除仅管理员。
- 用户提交链接按“内容+用户”生成任务ID；SSRF边界覆盖平台HTTPS白名单、私网/回环DNS、逐跳页面/媒体重定向、5处Playwright请求拦截，并让yt-dlp强制经过只连接已验证公网443 IP的临时CONNECT代理。
- 小红书登录改为服务器headless Chromium截取二维码；generation与锁阻止旧线程覆盖新二维码或登录态，成功/超时/切换会清理二维码。
- Docker使用Python 3.11 slim、Chromium only、opencv headless、Gunicorn单worker、非root UID 10001；Compose不映射5000端口，限制并发/OCR/Web线程1、CPU 1.5、内存1536MB、PID 256和shm 256MB。
- SQLite、Cookie和输出挂载到仓库外 `/opt/ideahub-collector/`；服务重启会把未完成任务标为可重试的 interrupted，不静默重复执行。
- IdeaHub“数据”分组新增“内容采集”：服务与账号状态、二维码、链接提交、任务轮询、历史、结果图片/评论/互动、AI白名单编辑、刷新、推送真人/矩阵、管理员确认删除全部接入。

**代码级验收**：Collector代理27项、Compose断言14项、Python数据库4项、链接工具3项、安全单元5项通过；前端生产构建约300KB。浏览器实测任务 pending→running→done、AI编辑保存、真人推送、二维码 waiting_scan→saved、管理员删除确认均通过。桌面无横向溢出；390×844手机视口 `scrollWidth=380 <= 390`、开始按钮46px、历史与结果单列，浏览器console为0。

**尚未验收/禁止合并 main**：本地未构建Collector镜像，也未运行真实Chromium二维码、FFmpeg、平台采集或AI调用。必须先把功能分支推送，在VPS执行 `bash scripts/check-collector-vps.sh` 并完成一条真实小红书/抖音采集与IdeaHub推送；通过后再合并main。

## 2026-09-01：新增项目功能树与 AI 协作介绍

“总览”分组在“今日工作台”下新增独立的“项目功能树”入口，用一页展示 IdeaHub 的 8 个功能域、19 个核心页面和关键二级能力，帮助新人理解项目全貌，也让现有成员快速定位功能。

- 8 个一级功能域默认展示二级目录，每个功能域可独立收缩，也支持全部收起或展开。
- 22 个二级入口统一标记 AI 协作，并补齐项目用途、AI 三项具体职责、人工确认边界和核心产出。
- 点击二级入口不会立刻跳页，而是在所点击功能域的紧邻右侧插入就地介绍卡；确认后再进入真实模块。全局搜索、AI 整理和两个学习入口复用已有交互。
- 功能搜索同时匹配模块名称、项目介绍、AI 职责、人工边界和产出；手机端自动改为单列，介绍卡紧跟所点击模块下方。
- 本次仅增加前端视图、导航和样式，不涉及数据库、接口、环境变量或 Docker 配置。

**验收**：`function-tree.js` 与 `main.js` 语法检查、生产前端打包和 `git diff --check` 通过，打包后前端约 619KB。桌面 4 列区域宽 910px、无横向溢出；点击“灵感池”后介绍卡与所属功能域同顶、间距 13px，三项 AI 职责完整。功能搜索“直播”只保留内容运营与真人直播；模块收缩和“进入该模块”跳转正常。390×844 下主区 `scrollWidth=380 <= clientWidth=380`，结构树单列、22 个 AI 标识完整、介绍卡紧跟模块下方，浏览器无新增错误或警告。

## 2026-09-01：建立项目级 Agent Skills 与可重复 UI QA

把原先 175 行、每次任务都整份加载的 `AGENTS.md` 拆成 65 行全局安全与路由规则，并在 `.agents/skills/` 建立前端、UI 验收、后端、数据库、Collector 和发布六个按需 skill。`ideahub-frontend` 固化原生 ES Module 架构、当前视觉语言、响应式、API/mock 与异步状态规则；`ideahub-ui-qa` 固化完成门禁和专项测试选择，其余 skills 承接原文件中的权限、迁移、采集与 VPS 高风险规则。

原 `npm run test:ui` 因仓库没有 Playwright/Sharp 而无法启动，且早期静态原型已经不能代表当前产品。现已改为复用 package-lock 内的 esbuild 与 Puppeteer：测试在内存中构建生产 bundle，使用随机本地端口和内置 mock，不改写 `web/index.html`，检查 8 个核心页面、1440×900 与 390×844、横向溢出、移动导航、提交弹窗、reduced motion、脚本异常与 console error，并把 8 张截图及 `report.json` 写入被忽略的 `scripts/.uidiff/`。

新增 `npm run benchmark:skills`：baseline 只加载全局规则，with-skill 加载任务命中的 skills。6 个确定性契约场景为 100% 对 15%，常驻规则减少 62.9%；六个 skill 共附 14 条行为 eval 提示，结果保存在 `docs/agent-skills-benchmark.*`。该指标只证明任务规则进入上下文，不冒充模型质量跑分。

**验收**：六个 `SKILL.md` 全部通过 skill-creator 格式校验；7 个 eval JSON 可解析；`ui-check.mjs` 与 `benchmark-agent-skills.mjs` 语法通过；UI QA 19 项检查、8 张截图、619KB bundle、0 个浏览器错误；关键桌面与手机截图人工复核通过；skills benchmark 连续运行产物哈希一致；`git diff --check` 通过。此次没有新增依赖、数据库迁移、环境变量或 Docker/Caddy 改动，属于 A 类。Collector 功能分支的 VPS/真实平台门禁仍保持，不因本次开发工具改造而自动解除。
## 2026-09-04：建立项目级 Skills、统一 UI 门禁与真实 Agent 评测

本次从 `origin/main` 建立独立的 `feat/project-quality-system`，只承载可回合并主线的开发质量能力，没有把尚未进入 main 的 Collector 业务代码带入。根目录只保留始终生效的安全与同步规则，前端、UI 验收、后端、数据库、Collector 和发布流程分别放入 `.agents/skills/` 按任务加载。

- 原“字符串命中 benchmark”改名并收口为 `lint:skills`，只检查 Skill 结构、引用与静态安全合同，不再冒充模型质量分数。
- 新评测支持真实 Agent 的无 Skill / 有 Skill 规划 A/B、随机匿名后的独立盲审、仅凭 metadata 的触发路由，以及一次性夹具中的实际修改。候选代码只允许在无网络、只读根文件系统、固定镜像和 Node permission model 的容器内验证；缺少容器或实际写权限时直接报环境失败，不把未修改结果算成 Agent 能力失败。
- 生产构建和 Puppeteer 共用 `scripts/lib/web-build.mjs` 与 `scripts/lib/ui-harness.mjs`。通用 UI QA 自动发现当前分支全部导航入口，并检查桌面/390×844、横向溢出、console/网络错误、弹窗焦点、44px 触控、页签方向键与 ARIA 关联。
- GitHub Actions 只对可信 push 与人工触发运行；仓库历史仍含生产配置，因此不开放 PR 代码执行。第三方 Actions 均固定到不可变提交，并在执行仓库脚本前移除 runner 上的 Git 对象库。
- README、`npm run setup` 和演示录制说明已按当前依赖与 Puppeteer 实现修正；原生前端模式库增加页面壳、视图模块、弹窗、最新请求获胜、roving tabs、响应式数据和专项 UI 测试起点。

**验收**：全部纳入提交的 JavaScript 文件语法通过；Skill lint、评测计划验证、构建 round-trip 与 12 项评测工具自测通过。UI QA 通过 112 项检查、15 个 main 导航能力和 5 张桌面/手机截图。最小真实规划 A/B 中，独立盲审判定 baseline 通过 3/5、加载 Skills 后通过 5/5，并正确选择 `ideahub-frontend` + `ideahub-ui-qa`。本机 Codex Desktop 内再启动 Codex CLI 时，外层策略使实际修改会话只读；该环境限制已由写探针识别为 runner failure，未伪装成执行质量结果。无数据库、`.env`、依赖版本或 Compose 变更。
